# Impact map — no authentication redirects

| Symbol | Consumers | Verification |
|---|---|---|
| new `http_transport.open_without_redirects` | credential probe, Gemini Developer, Vertex | real loopback redirect never reaches target |
| `credentials._https_get` | key validation/status/panel | shared opener and bounded 3xx refusal |
| `gemini._https` | token counting, Path A, Stage 4, Vertex | shared opener; no retry or auth regression |
| README module map | operators/reviewers | new module is documented and map remains exhaustive |

No real credential, cloud request, fixture, golden, workflow, or model asset is used or changed.
