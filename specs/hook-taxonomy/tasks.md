# Tasks: Hook Taxonomy, Payoff and Loop Scoring (Task T4.4)

- [x] T1 Update `JudgeVerdict` and `Editorial` models with taxonomy and payoff fields
- [x] T2 Update `VERDICT_SCHEMA`, `_PROMPT`, and candidate tiebreaking in `pipeline.py`
- [ ] T3 Add unit tests in `tests/test_judge.py` and `tests/test_clip.py`, pass `verify.sh`, and update ledger
