# Impact map — bounded Path B refusal diagnostics

| File | Symbol | Required proof |
|---|---|---|
| `src/hawedit/path_b.py` | `UnreadableScene.__post_init__` | type, blank, normalization, exact bound, ordinary-message control |
| `tests/test_path_b.py` | record-level regressions | JSON-safe bounded reason and non-string refusal |
| `tests/test_video_reader.py` | real `read_scenes` refusal route | exception type retained, control/tail removed, sibling readings preserved |
| `security/wsl-asr-vex.json` | source applicability | exact reviewed package digest after the source change |

No CLI, data schema key, model request, retrieval score, candidate identity, or delivery artifact
changes. Existing consumers continue reading the same `reason: str` field.
