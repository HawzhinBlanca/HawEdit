# Tasks: Face Detector Sample Rate and Tracker (Task T2.5)

- [x] T1 Update `OpenCvFaceTracker` default `sample_fps` to 5.0 and implement between-sample tracking
- [x] T2 Add unit tests in `tests/test_reframe.py`, pass `verify.sh`, and record in ledger
