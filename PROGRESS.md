# PROGRESS — Kurdish Video Repurposing System

Generated from `BLUEPRINT.md` §9 (Build order). **The agent never marks a task DONE by
judgment**: DONE requires code + test + the gate green + evidence linked below.

- Gate: `bash hawedit/scripts/verify.sh` (lint + format + typecheck + tests)
- Blocked items: `BLOCKED.md` · Deviations: `DECISIONS.md`

## Status legend

| Mark | Meaning |
|---|---|
| DONE | Gate green **and** evidence recorded below **and** the task's stated Definition of Done met in full |
| PARTIAL | Gate green on what was built, but the Definition of Done is *not* met — the shortfall is named in the evidence column. Introduced after the 2026-08-07 audit found DONE marks that were really this. |
| WIP | In progress this iteration |
| TODO | Not started |
| BLOCKED | Needs Hawa / hardware / credentials — see `BLOCKED.md` |

## Milestones (§9)

| Milestone | Deliverable | Blocks | Status |
|---|---|---|---|
| **M0** | ASR benchmark harness + labelled Sorani audio set | Everything | **harness and canonical adapter built and gated; real Sorani measurements remain blocked by the absent labelled corpus** |
| **M1** | Stage 0 + Stage 1 → raw/normalized transcript with word timings | M2 | **WIP — Stage 1 is complete and has run through the CLI with LLM+CTC, validator routing and Viterbi timings; Stage 0 remains partial because diarization is gated** |
| **M2** | Vertical slice: transcript → BM25 → Gemini → manual boundary → one rendered clip | Proves the concept | **WIP — index, boundary fusion, §5 contract and a real rendered clip DONE; only Gemini (Stage 3) is still missing from the slice** |
| **M3** | Stage 6 render path with verified RTL captions + golden test | Client delivery | **WIP — §4.3 and NVENC run; dynamic face-continuity crop is wired; active-speaker association remains gated; delivery set DONE** |
| **M4** | Stage 3 Path A (full-transcript discovery) | Verbal recall | **DONE — delivered in M2.3 (`path_a.py`, 21 tests). The milestone row said TODO while its own ledger row said DONE; the drift is the point of the status legend and is corrected here.** |
| **M5** | Stage 2 visual index + Stage 3 Path B | Visual recall | **DONE in code — one composed owner extracts once, retrieves top 50, reranks, keeps 5–10 and sends only survivors to VideoChat3; the unranked injection seam is refused. Quality still needs M7.** |
| **M6** | Stage 5 TimeLens2 + sentence-hard fusion | Boundary precision | **DONE in code — the runner grounds the selected transcript against overlapping windows, shifts intervals to media time and fuses only relevant evidence. Quality still needs M7.** |
| **M7** | Repurposing eval set + threshold tuning | Quality gates | **WIP — §8.2 metrics DONE; the 200–500 labelled candidates need humans** |
| **M8** | Auto-reframe (SAM 3 / Molmo2) | Vertical formats | **BLOCKED — `BLOCKED.md` #9. Neither SAM 3 nor Molmo2 is in §7. §3 Stage 6 says to add SAM 3 *only if* face-centred cropping proves insufficient on real footage, and that prerequisite is now built and has its own row (M8.1, PARTIAL) — so the accurate statement is no longer "not startable": the step §3 asks for first is taken, and the *decision* it is supposed to inform cannot be made, because judging "insufficient on real footage" needs real footage (`BLOCKED.md` #1) as much as it needs Hawa's answer on the two models.** |

## Where this stops, and why

Everything that does not require credentials, model weights, a GPU, human annotators, or a
decision from Hawa is built, tested and gated. `bash scripts/setup.sh` takes a clone to a green
gate; `python -m hawedit.pipeline VIDEO.mp4` runs §3 as far as the available models allow and
exits non-zero naming every stage it could not.

**Re-assessed 2026-08-08: this checkout moved to hawapc01, and two of those six went away.**
The GPU is here (2×24 GiB, §6's own figure) and `huggingface.co` is reachable, both measured in
`evidence/hawapc01-environment.md`. That is a change of desk, not of code — but it makes three
rows startable that had been marked impossible, and it exposed one blocker the other two were
hiding.

| Waiting on | Blocks |
|---|---|
| ~~`huggingface.co` reachable~~ | **Resolved** — every §7 repo that has an id now serves files; measured per repo, not per host |
| ~~A GPU (hawapc01)~~ | **Resolved** — this *is* hawapc01, so NVENC in M3.3 and every model row lose their hardware reason |
| ~~Where §3 Stage 1 runs (`BLOCKED.md` #11)~~ | **Resolved** — Windows uses the explicit WSL2 bridge; Linux runs locally; setup and runtime selection are scripted and tested (D-064) |
| The §3 ZDR governance answer (credentials themselves are now handled) | confidential material only — `gemini.Governance` refuses it until ZDR is configured and attributed |
| The gated `pyannote/speaker-diarization-community-1` repo | M0.10, M1.3, the speaker-tracked reframe in M3.3 — measured **401** from here, so the network opening did not touch it |
| Human annotators + real footage | M7.2, and M7.3 behind it |
| Hawa, one decision each | `BLOCKED.md` #7 (required CI check) · #9 (M8 names two models §7 does not contain) · #8, #10 and #11 resolved, with `BLUEPRINT.md` §5 and §7 still needing matching amendments |

**All model stages are now composed in the runner.** Query → retrieve → rerank → bounded
VideoChat3 → automatic sentence anchors → keyframed judge → TimeLens → dynamic face crop is one
auditable route, and Windows Stage 1 has a WSL2 worker. What remains is measurement, not another
join. **Not** M0.16: it was
listed here this morning and is BLOCKED again, because the hosts answering 200 turned out not
to be the question — the interim corpus itself is no longer publicly distributed. And M7.3
sits behind M7.2. The contracts they plug into are built and tested ahead of them, so landing each
is a matter of producing the type its stage already expects.

## M0 — task ledger

M0 decomposed from §8.1 (ASR benchmark) + §4.1 (normalization is a prerequisite of
"normalized CER"). Decomposition is task breakdown, not architecture change.

| Task | Definition of Done | Status | Evidence |
|---|---|---|---|
| M0.17 | Local gate binds the canonical interpreter, installation and dependency identity to this checkout | DONE | `src/hawedit/environment.py` + `tests/test_environment.py` + `scripts/verify.sh`. External/token-forging `PY`, wrong-root/duplicate editable metadata, unsupported Python/project versions and active dependency drift are refused before grading. D-117; `evidence/environment-identity.md`. |
| M0.18 | Measured dual-3090-Ti CUDA dependency and hardware identity | DONE | `src/hawedit/gpu_runtime.py`, `requirements/host-gpu-windows-py311.txt`, and `tests/test_gpu_runtime.py`. The 46-wheel Windows/Python 3.11/CUDA 13.0 graph is hash-locked, packaged and exact-inventory audited; a fresh environment passed `pip check`, imported Torch 2.13.0+cu130/Torchvision 0.28.0+cu130/Torchaudio 2.11.0+cu130, and executed bfloat16 work on both 24 GiB RTX 3090 Ti cards. This qualifies the deployment runtime, not VideoChat3's 64-frame application path; that measured OOM remains M5.4/BLOCKED #17. `evidence/gpu-dependency-lock.md`. |
| M0.1 | Package skeleton + gate script; gate refuses a no-op command instead of printing green | DONE | `src/hawedit/gate.py` + `tests/test_gate.py` · gate green: `VERIFY OK — hawedit gate green` · found + fixed 2 real gate defects, see D-005. The floor compares `evidence.passed`, never `collected`: the two differ by exactly the skips, which is the case the ratchet exists to catch. `README.md` described it as *collected* until D-069. **Correction 2026-08-09:** the "refuses a no-op command" guarantee had a hole the size of the whole gate — the override refusal covers `LINT_CMD`/`FORMAT_CMD`/`TYPECHECK_CMD`/`TEST_CMD`, but `PY` prefixes all four *and* the evidence step, so `PY=/usr/bin/true.exe bash scripts/verify.sh` printed `VERIFY OK` in 1s, exit 0, with no report written. An interpreter now proves it can import `hawedit` before it is trusted to grade it (exit 3); mutation-audited 5/5. D-104, `evidence/py-override-bypassed-the-whole-gate.md`. |
| M0.2 | §7 model registry in code; model outside §7 rejected; NC licence hard-rejected | DONE | `src/hawedit/registry.py` + `tests/test_registry.py`. The tests **parse §7 out of `BLUEPRINT.md`** and assert exact set equality both ways, so a model added in code but not in the blueprint fails the gate. **Amended 2026-08-09 (D-087), found by the second adversarial pass:** that guarantee had two holes, both invisible to set equality because it compares the *cells each table self-declares*. (1) `resolve` reads `REGISTRY` before `EXCLUDED` and nothing asserted the tables were disjoint, so a model §7 **excludes** could be registered and would resolve — measured, `Whisper` with a cell §7 already contains, no role and an attribution-free licence returned from `resolve()` with no `ModelExcluded` and the full suite at `exit=0, 0 FAILED`; two of §7's nine exclusions are CC-BY-NC-4.0 hard rejects, so this could route work to a NonCommercial model. (2) A **duplicated** cell left the declared set unchanged, which is how that entry hid. `assert_registry_excludes_nothing_it_registers` now enforces both at **import** — a contradiction in the data, so unconstructible beats deciding which table wins, and it fails for library consumers rather than only in the suite. 15 entries / 15 distinct cells, so uniqueness was enforceable. 5/6 mutations; the survivor is the import-time call, redundant with two tests for gate purposes and kept for import-time refusal. Three attempts were needed to measure the defect: two mutations read CAUGHT for reasons unrelated to exclusions (a malformed append caught by mypy, an attribution-requiring licence caught by README bookkeeping) — a mutation caught for the wrong reason reads as protection that is not there. The stale `(15 tests)` count is dropped rather than restated. `evidence/registry-excluded-model-resolvable.md`. |
| M0.3 | §4.1 Sorani normalization via KLPT; every §4.1 collision asserted in a test | PARTIAL | `src/hawedit/normalize.py` + `tests/test_normalize.py` + `tests/test_waw.py`. Four of §4.1's five collisions are handled: ZWNJ, Farsi/Arabic `ی`/`ک` and Numerals by KLPT (D-003), and conjunctive `و` by M1.7 against KLPT's dictionary (D-026). **Demoted from DONE 2026-08-09 (D-076), found by the adversarial pass.** This cell read *"All five §4.1 collisions are now handled … the fifth — conjunctive `و`"*, which reached five by counting §4.1's single Numerals row twice. Read out of the frozen blueprint, `BLUEPRINT.md:228-232`, conjunctive `و` is row **four** and row five is `Diacritics ř / ł` — "Normalize in Latin-script material". **Shortfall (one, named):** row five is unimplemented — measured, `normalize_sorani` leaves `řoj baş` and `łe gułan` byte-identical and no file in `src/` or `tests/` mentions either character — and it is not implementable without a decision, because §4.1 does not say what `ř`/`ł` normalize *to* and in Kurdish Latin orthography they are distinct phonemes rather than decorated variants. Refused rather than guessed: `BLOCKED.md` #13. `evidence/adversarial-pass-2026-08-09.md`. |
| M0.4 | Kurdish invariants #1 and #3 in code: `transcript.raw.json` write-once, model inputs read `norm` | DONE | `src/hawedit/transcripts.py` + `tests/test_transcripts.py`. #1 enforced three ways (refuse-rewrite, frozen types, SHA-256 tamper evidence); #3 enforced by distinct types (mypy) + `assert_model_input` (runtime) + stale-norm detection. ASR provenance is validated against §7 at construction. **Hardened 2026-08-10 (D-166):** `write_norm` no longer writes directly onto its predictable final name. A planted hardlink previously redirected the normalized JSON into an external victim, and an interrupted rewrite could leave truncated model input. Publication is now serialized with the raw transcript, verifies the raw and the norm's source digest before and immediately before publication, fsyncs a private sibling, and atomically replaces the final name. Failed publication preserves the last complete norm; cleanup failure cannot mask the primary error. `evidence/adversarial-pass-21-2026-08-10.md`. **Hardened 2026-08-10 (D-167):** the store root itself is now a lexical absolute, lstat-bound real directory. A pre-existing symlink/junction is refused and the directory identity is rechecked before and after each publication lock and around reads, so replacing `work/transcripts` cannot redirect raw, digest, lock or norm artifacts outside the declared store. `evidence/adversarial-pass-22-2026-08-10.md`. |
| M0.5 | §8.1 accuracy metrics: normalized CER, spacing-free CER, named-entity error, code-switch error | DONE | `src/hawedit/metrics.py` + `tests/test_metrics.py`. Definitions §8.1 left open are recorded in D-008. Unmeasured returns `None`, never 0.0. **Amended 2026-08-09 (D-089), found by the second adversarial pass:** that last sentence had a hole. `""` is a substring of every string, so a **blank** entity satisfied `entity in hypothesis` and counted as a name that *survived* — `named_entity_error_rate("سەرۆک لە شار", ("",))` returned **0.0**, the same value as a name transcribed perfectly, and `CorpusItem(..., conditions={NAMED_ENTITIES}, named_entities=("",))` constructs without complaint, so one blank label silently inflated §8.1's accuracy. The sibling `code_switch_error_rate` already refused the identical input as "a corpus defect", so the rule was extracted rather than invented: `_normalized_annotation` is shared by both, message shape unchanged. An *empty tuple* still returns `None` — nothing annotated is not malformed data, and a mutation collapsing the two is caught. **Also closed:** D-008 claims all four of its recorded choices are tested; the fourth (exact matching — "a name 90% right is still the wrong name") was not, so a fuzzy 0.34 threshold could have been introduced with the suite green. Now pinned in both directions, since a near-miss must score 1.0 *and* an Arabic-keyboard `كوردي` must still match `کوردی`. 6/6 mutations — 5/6 at first, and the survivor exposed a second unprotected guard: removing the code-switch refusal, the one implemented correctly all along, also left the suite green. The stale `(26 tests)` count is dropped rather than restated. `evidence/blank-annotation-scored-as-found.md`. |
| M0.6 | Labelled-corpus manifest + §8.1 coverage validation (3 dialects × 7 conditions), per-dialect never aggregated away | DONE | `src/hawedit/corpus.py` + `tests/test_corpus.py`. Missing cells are named, not counted; hours are reported per dialect per §4.4; the hours floor is D-009. **Amended 2026-08-09 (D-091), found by the second adversarial pass:** the grid **certified itself**. `tests/test_corpus.py` referenced `BLUEPRINT.md` nowhere and compared the `Dialect`/`Condition` enums against literal sets typed into the test, so an eighth §8.1 condition or a fourth dialect was invisible — while this row claims "(3 dialects × 7 conditions)" implements §8.1's list. §8.1's coverage line is now parsed out of the frozen blueprint with set equality both ways, as `test_registry.py` has always done for §7. The phrase→member mapping is **explicit** rather than counted, because §8.1 yields **nine** items against a **seven**-member enum: "Kurdish–English and Kurdish–Arabic code-switching" is one phrase covering two members — the same shape as §4.1's single "Numerals" row, which is exactly how M0.3 came to claim five collisions handled when four were (D-076). Separately, `MINIMUM_HOURS` was referenced by **no test**, so D-009's recorded 3.0 could drift from the code — 3.0→1.0 left the whole suite green; the value is now parsed from D-009's heading, so changing the floor requires amending the record. 4/5 mutations on the grid plus the floor drift caught; the survivor is D-078's neutral class (retyping the parse is unobservable while the blueprint is frozen). `BLUEPRINT.md` was touched to simulate §8.1 growing and restored, verified by sha256 and `git status`. The stale `(19 tests)` count is dropped rather than restated. `evidence/section-8-1-grid-self-certifying.md`. |
| M0.7 | ASR adapter interface + throughput harness: RTF, peak VRAM, long-audio failure rate | DONE | `src/hawedit/asr.py` + `tests/test_asr.py`. `Hardware` is required and cross-hardware comparison is refused per §3 Stage 1; failures are recorded not raised; every measurement names its adapter class. |
| M0.8 | Alignment-accuracy metric against CTC emissions (§8.1 last metric) | DONE | `src/hawedit/alignment.py` + `tests/test_alignment.py`. Kurdish invariant #5 enforced at construction in `AsrProvenance`/`RawTranscript`, not just at scoring. |
| M0.9 | Benchmark runner → comparable report JSON + §8.1 decision rule (LLM-7B stays canonical unless material gain) | DONE | `src/hawedit/bench.py` + `tests/test_bench.py`. Five clauses enforced, per-dialect always reported alongside the aggregate, thresholds recorded in D-010. **Corrected 2026-08-10 (D-141):** `AUDIT_REPORT.md`'s verification evidence claimed *“`hawedit`, `hawedit-asr-bench`, `hawedit-editorial-bench` and `hawedit-asr-setup` all start from the installed wheel”* — **four**, while `[project.scripts]` declares **five**. The omitted one is **`hawedit-credentials`**, the entry point that handles the API key; it arrived with M2.8 and the sentence was never re-derived, the same uncounted-list failure as D-127's *five repositories* and D-129's *four blocked stages*. **The claims were true and the list was short:** verified against a wheel built from this tree and installed into a fresh CPython **3.12.13** — all **5** console scripts present and exiting 0 on `--help`, `pip check` clean, and a **346,694-byte** wheel of **55** entries carrying `assets/fonts/NotoNaskhArabic-Regular.ttf`, `assets/fonts/OFL.txt`, `models/revisions.json` + `models/sources.json`, `hawedit/asr_worker.py` and `hawedit/wsl_setup.py`. The sentence is now bound to `[project.scripts]` by **set equality both ways**, with the count asserted separately because a list can be right while the number beside it is stale, and the wheel-contents claim names paths instead of categories. **6/6 mutations**, after 4/6 — and one of the two survivors was **the primary defect itself**: dropping `hawedit-credentials` from the list left the suite green because the correction note in the same bullet *names* it, so the check read it from the explanation. That is **D-121's prose-grep trap for the third time** (after `fetch-ffmpeg.sh`'s `--fail` comment and D-139's quoted install command); the test now reads the bullet only up to ` **Corrected 2026-08-10 (D-142):** every entry point's `--help` named something the reader cannot type, in one of its two invocation modes — D-141 recorded two of them in passing and the measurement found all five. From the real wheel: `hawedit --help` printed `usage: hawedit.pipeline` and `hawedit-credentials --help` printed `usage: hawedit.credentials` — module paths, not commands, because both set a fixed `prog=`. Under `python -m` the other three printed `bench.py`, `wsl_setup.py` and `editorial_bench.py` — argparse's `basename(sys.argv[0])`, also not commands. **Five for five wrong in one mode**, and argparse reuses `prog` in its error messages. `cli.program_name()` now derives it from `sys.argv[0]`, which Python sets to the module's *file* under `-m` and to the script otherwise, so a `.py` suffix separates the two with nothing guessed: `python -m hawedit.pipeline` (the form this repo's docs use) or `hawedit` (the script's name, `.exe` stripped). It lives in `cli.py` beside `use_utf8_streams` for the same reason, and all **six** parsers use it including `smoke.py`, which has no console script — so the next module to gain one need not remember. An empty `argv[0]` falls back to the module path, because `Path(“”).stem` is empty and `usage:` naming nothing is worse than either. Verified on the artifact: **10 of 10** invocations now name a command that exists, from a freshly built wheel on CPython 3.12.13 and from `-m`. **9/9 mutations**, with the control being that the two modes must print *different* names — a fixed `prog` satisfies one assertion, so checking only one would be satisfied by the defect. Rejected `sys.executable -m …` (an absolute interpreter path, different on every machine) and leaving `prog` unset everywhere (right from the wheel, wrong under `-m`). **The first push was refused by CI and the fault was mine, not the code's:** the fake `argv[0]` was a `C:\…\Scripts\x.exe` literal, and on POSIX `\` is not a separator, so `Path.stem` returned the whole string — five cases red on the runner, green here. The rule holds on both platforms; the fixture was Windows-only. Rebuilt with `Path(...)` and parametrised over the bare and `.exe` shapes, which checks more than the original did. Second platform-bound test of mine after D-137's skip. `evidence/help-named-a-command-that-was-not-installed.md`. **Corrected 2026-08-10 (D-143):** `README.md` — the last document in step 1(b)'s list never checked systematically, and the one a reader meets first — **understated this project's own bar**. It said *“Making that job a required status check is a repository setting, and is not done”* for two days after `BLOCKED.md` #7 was **RESOLVED 2026-08-08**. Measured against the live API rather than the record: `required_status_checks == {“contexts”: [“gate”], “strict”: true}` — required *and* branch-must-be-current, stronger than the sentence claimed, and every `git push` in this loop prints `Required status check “gate” is expected.` **Second finding: the `cli.py` module-map row named 0 of 3 exports** — it described `use_utf8_streams`'s effect without naming it and omitted both `machine_readable_stdout` (D-119) and `program_name`, which D-142 added **two commits earlier**. Bound now: a resolved `BLOCKED.md` entry cannot be called undone **and** a live one cannot be called done (both directions, since overstating is worse), and the `cli.py` row must name every callable in `hawedit.cli.__all__`. Doc-to-doc deliberately: reading branch protection needs the network and a token, which is a test that skips. Rejected checking all forty-odd module-map rows against their `__all__` — most describe an invariant, not an API, and the check would make them worse documentation. **7/7 mutations**, after 6/7: the survivor was the mirror direction, which returned early on a live entry instead of asserting the opposite. **And the prose-grep trap struck a fourth time** — the fix's own correction quoted “is not done” — so `tests/test_claims.py` now has one `claims_only()` helper that strips ` **Corrected**`/`**Amended**` spans, used by both documentation checks, after D-121, D-139 and D-141 each fixed it locally. Its own first version dropped whole paragraphs and emptied the audit report's list; it cuts at the marker now. `evidence/the-readme-understated-its-own-bar.md`. **Corrected 2026-08-10 (D-154):** `AUDIT_REPORT.md`'s first Secondary-debt bullet said *interrupted delivery ... refused rather than repaired in place* — the opposite of the shipped behaviour since **D-146** landed the same day, and it stood for two days. Measured on this tree: an abandoned attempt (artifacts, no completion record) is **accepted** and its names come back in `PipelineRun.resumed_over`; only a set whose record exists and whose byte lengths match still raises `FileExistsError`. Corrected in place with the marker this file already uses, keeping the debt that genuinely remains (two *simultaneous* runs of the same media id and selection are no longer caught at the pre-write guard — D-146 records that trade). Rejected deleting the bullet, which would leave a reader unable to tell a fixed problem from an unnoticed one. **The binding matters more than the wording:** the new test *runs* the guard on both states and only then requires the audit's wording to match, because a grep-only test passes with the code reverted and the prose intact. **A second binding for the class:** nothing checked that a `D-0NN` cited in the docs exists — measured, **182 citations** (README 8, AUDIT_REPORT 8, PROGRESS 133, BLOCKED 33), all resolving; the check earned itself immediately by failing on `['D-154']` when the marker was written before the entry. **6/6 mutations after 1/4** — the load-bearing one reverts the code and leaves the prose, and two first-pass survivors were my own work: my correction put the truth *after* the ` **Corrected**` marker, but `claims_only` keeps what *precedes* it, so the text the test read as live was the struck-through **false** claim (which contains `repaired in place` inside `refused rather than repaired in place`) — the prose-grep trap inside the test written to prevent it, fifth in the project after D-121/D-139/D-141/D-143 and the first inside a test; and the document list was hard-coded, so dropping `AUDIT_REPORT.md` from the citation check went unnoticed (D-149's lesson, one iteration later). Both fixed: the bullet is rewritten live-claim-first, the test also refuses the contradicting phrases, and the list is derived from `ROOT.glob("*.md")`. No production code changed. Floor 1499 → 1501. `evidence/an-audit-that-described-the-opposite-of-the-code.md`. **Audited 2026-08-10 (D-156) — the gate's own nine refusals swept, 7/9.** This is the row everything else rests on, and the largest never attacked as a whole. **The real gap:** `if evidence.passed == 0` — the guard that exists because *700 collected, 700 skipped* once printed VERIFY OK with no test bodies run — was held by nothing. Replacing it with `passed < 0` left all 1,503 tests green, because **every existing test supplies a non-zero floor** and at a non-zero floor the *floor* check refuses first. Its load-bearing state is a floor of **0**, which `read_floor` returns for a missing or empty file, and nothing paired the two: measured with the guard neutered, `floor missing (0)` **ACCEPTED collected 700, skipped 700, passed 0**, while `floor = 1503` was refused by the floor. Three states now pin it (missing, empty, whitespace-only), each asserting `read_floor(floor) == 0` first so the test cannot pass because the floor refused instead, with a control requiring a healthy 700-with-one -skip report to be **accepted** at a zero floor and to ratchet to **699** — which also re-pins D-095's collected-versus-passed distinction. Rejected making `read_floor` refuse a missing file (a new checkout has no floor until the first green run writes one, and the floor is not the mechanism that should catch a self-skipping suite — two guards, two jobs) and rejected pinning refusal messages (D-072's rule). The second survivor is a **bad mutation of mine**, recorded not pinned: removing `if not suites:` still refuses via the `collected == 0` guard — only the wording changes. **8/9 after**; no production code changed, `gate.py` byte-identical. Floor 1503 → 1507. `evidence/the-gate-guard-that-only-matters-when-the-floor-is-zero.md`. **Audited 2026-08-09 by adversarial pass #7 (D-107), against the real 38-minute run's artifacts rather than fixtures:** on the actual 820,835-byte `transcript.raw.json` the sidecar matched, a second write of *identical* content was refused, one digit changed mid-file was detected, a norm from a different raw was refused, and the file really is `-r--r--r--` `0o444` — **0 invariants broken**. The gap was in the tests: `write_raw` refuses twice over and only the first refusal was reached, so neutralising the raw file's own `os.link(staging, path)` left the whole suite green. That layer is the one that matters when the sidecar is **gone** — the state someone hiding a modification would create, since `verify_raw_integrity` needs the digest to detect anything. Now tested in that state (refusal, unchanged bytes, no resurrected sidecar, no staging leak) with a control pinning the first layer to its own message. 5/5 mutations after the fix. `evidence/adversarial-pass-7-2026-08-09.md`. **Corrected 2026-08-12 (D-192):** invariant #1 held and the check built on it did not. `read_norm` decided whether a norm belonged to its raw by re-serialising the *parsed* raw and hashing that, so it answered with today's schema rather than the stored bytes — D-181 adding one optional `adapter` field re-dated every transcript ever written. Measured on four real artifacts including the 38-minute run: `verify_raw_integrity` **passed** (bytes `7912e7bd1d35…` = the digest recorded at write time) while `read_norm` refused the same file as *“derived from raw 7912e7bd1d35… but the stored raw is 4748ac2a3e02…”* — a hash of a file that has never existed. Its prescribed remedy re-stamped the same unstable value. Nothing reached a client (the pipeline writes and reads the norm inside one run) but 35,185 chars of Kurdish and 6,104 word timings were unreadable through the API that exists to read them. Identity is now the digest of the **file** in all four places that touch it, `write_norm` refuses a norm stamped any other way, and all four artifacts read again. **3/4 mutations caught**; the survivor — the pipeline call site — is unfalsifiable for a same-release artifact, where both digests are the same number, and is recorded rather than covered by an assertion about the spelling of a line. `evidence/adding-a-field-re-dated-every-transcript-ever-written.md`. **Audited 2026-08-09 by adversarial pass #17 (D-130): 7/9.** Held: the long-audio rate returning `None` rather than 0.0 when no item is long, its refusal of mixed hardware and `assert_one_hardware` itself, RTF as wall-clock-over-duration, `failed` reading `result is None`, the per-measurement `peak_vram_bytes` staying `None` without a probe, and D-097's module-qualified adapter name. **Both survivors were the hard rule one layer up, in the aggregate that gets written and read.** `mean_rtf … if self.scores else 0.0` is not a missing number but *infinitely fast* — the most flattering wrong answer in the field §3 Stage 1 warns about — with `worst_rtf` reading 0.0 beside it; and `peak_vram_bytes=max(vram) if vram else 0` reads as *measured, and it used none* for a model §7 sizes at ~17 GiB, which is the figure §6's two-GPU layout is derived from. Both now asserted on `to_dict()`, the document a capacity plan is read off, with a control that a probed run still reports 17 GiB and a scored run still reports its mean and worst RTF — returning `None` unconditionally would satisfy the tests and erase every real measurement. My first VRAM test constructed a `ModelReport` directly and so sat one call away from the aggregation the mutation is in; it passed with the mutation in place and the re-run audit is what said so. 9/9 after; no production code changed. `evidence/adversarial-pass-17-2026-08-09.md`. **Corrected 2026-08-09 (D-131):** this row's whole deliverable was **computed and discarded**. `_score_item` scores alignment for every item with reference timings and a timed hypothesis and stores it on `ItemScore.alignment` — and `ModelReport.to_dict()` emitted no alignment field at all. Measured on six timed items: every one carried **matched 2/2, onset 30.0 ms, offset 30.0 ms, within 1.00 @ 50 ms, coverage 1.00**, while the substring `align` appeared **nowhere** in the written JSON. So §8.1's *last metric* never reached the document §8.1 is — the shape of D-070's `natural_silence_ms` and D-109's `mean_logprob`, found by following D-130's discovery that the aggregate layer is where a metric goes missing. `ModelReport.alignment` now carries it, weighted by **matched words** (as `_micro_cer` weights by characters — a two-word item and a sixty-word one are not equal evidence about timing), with **coverage** beside the errors because `AlignmentAccuracy` says why, `None` rather than a zero when nothing aligned (0.0 ms is the *best possible* score), and **two tolerances in one report refused** rather than averaged. D-094's recorded field-by-field schema went red the moment the key appeared, which is what made adding it deliberate. 5/5 mutations, the last by a control that shifts the hypothesis past the 50 ms bar and requires the rate to move from 1.00 to 0.00. `evidence/section-8-1s-last-metric-never-reached-the-report.md`. **Audited 2026-08-10 (D-158):** pass 25 disabled this row's six claims one at a time; five held and one did not. The **matched-word weighting** — stated here, in `ModelReport.alignment`'s docstring and in D-131 — was held by no test. `_TimedAdapter` shifts every item by the same amount over identical two-word items, so a weighted mean and a mean of per-item means are arithmetically the same number (measured: **30.0000 against 30.0000**), and `sum(x * w) / sum(w)` could be replaced by `sum(x) / len(x)` with the whole suite green. The code was right — no delivered report carried a wrong figure — but the guard was missing, and a refactor would have shipped a §8.1 error **6.5x too large** past a green gate. `test_the_alignment_aggregate_weights_by_matched_words_not_by_item` now drives `run_benchmark` over a 2-word item 200 ms out and a 20-word item 10 ms out returning only 10 of its words, so coverage is 12/22 and the three candidate weightings differ: **41.6667 ms** by matched words, 27.2727 ms by reference words, 105.0000 ms as a mean of means. The latter two are asserted against by name. **7/7 mutations**, after a 5/6 whose survivor was reference-word weighting on a fully-covered fixture — the same blindness one level down, in the fixture written to expose it. `evidence/the-alignment-aggregates-weighting-was-claimed-and-never-measured.md`. |
| M0.10 | Diarization benchmark: Community-1 vs 3.1 DER on Kurdish multi-speaker material | PARTIAL | `src/hawedit/diarization.py` + `tests/test_diarization.py`. DER with optimal speaker mapping and a reported breakdown, plus §8.1's boundary-reconciliation metric against word alignment. Control-model handling: D-011. **Shortfall (audit #10):** this is the *metric*, not the benchmark. No DER has been computed on Kurdish multi-speaker material — that needs the gated Community-1 weights (`BLOCKED.md` #4) and multi-speaker audio (`BLOCKED.md` #1, #6). The task as written is not done and cannot be until both clear. |
| M0.11 | Real-model adapters (`LLM_7B_v2`, `CTC_3B_v2`, `LLM_Unlimited_3B_v2`, `rzgar-ckb-v1`, Gemini native audio) | PARTIAL | Canonical LLM-7B + CTC-3B/Viterbi and the rzgar validator are composed behind official loaders and have run through the Windows WSL2 CLI route on hawapc01. **Shortfall:** Unlimited-3B and Gemini native-audio benchmark adapters remain absent; Gemini also needs `BLOCKED.md` #3. |
| M0.12 | Labelled Sorani audio set — several hours, per §8.1 category list | BLOCKED | `BLOCKED.md` #1 |
| M0.13 | Benchmark executed on real Kurdish audio on hawapc01; numbers recorded | BLOCKED | `BLOCKED.md` #1 (no labelled audio). The executable canonical route now exists through WSL2 on the same 2×24 GiB host; no CER/RTF is claimed until the real corpus run exists. |
| M0.14 | Public-corpus importer (Common Voice `ckb`) producing an interim, unlabelled manifest | DONE | `src/hawedit/corpus_import.py` + `tests/test_corpus_import.py`. Refuses to invent dialect, condition or duration. Authorised in D-012. **Correction 2026-08-09:** the fourth refusal — locale — was skipped for any row whose `locale` was absent or blank, so a Kurmanji split imported clean under a `ckb` provenance built from the parameter rather than the data (measured: 2 items, `reference_ckb='Ev pir bas e'`). Fixed and mutation-audited 3/3; the over-strict direction is held by an honest-`ckb` control. D-103, `evidence/common-voice-locale-bypass.md`. |
| M0.15 | Measure §4.1 collision incidence on real Sorani | DONE | `src/hawedit/collisions.py` + `evidence/collision-incidence.md` — 24,894 real entries; 0.21% of distinct forms would have failed to match. Surfaced a collision §4.1's table omits (D-013). Driven by `scripts/measure_collisions.py`. **Corrected 2026-08-12 (D-188):** the import **shrank the corpus in silence**, against the rule this module states in its own refusal for a missing duration — *"skipping it silently would quietly shrink the corpus"* — and against its sibling `import_cortex_speech`, which counts `unconfirmed` into the manifest under the comment *"Skipped, and counted"*. Measured by executing the importer on a four-row TSV with two unusable rows: **4 rows in, 2 items out**, and the provenance note named neither a skip nor a count — while corpus size is the denominator of §8.1's hours-of-coverage. The note now carries *"2 row(s) skipped as unusable … 2 of 4 rows read"*, reported **even at zero** (D-110) and with the denominator, since a count without a total says how many were lost but not out of what. The TSV is constructed rather than downloaded — there is no `ckb` release on this machine — which is why this was deferred six iterations rather than reported on sight; the defect is in the code's shape and the reproduction is by execution. **3/3 mutations.** `evidence/the-common-voice-import-shrank-the-corpus-in-silence.md`. **Corrected 2026-08-10 (D-160):** that last sentence was the whole problem — the script it names could not run on this machine, so a DONE row rested on a measurement nobody could re-derive. Two independent failures, neither covered by a test. `KLPT_DIC` was assembled as `.venv/lib/python3.11/site-packages/…`, a POSIX venv layout Windows does not use (`.venv/Lib/site-packages/…`, capital L, no version segment), so the script died with **FileNotFoundError, exit 1** before measuring anything — while the file sat at `.venv/Lib/site-packages/klpt/data/ckb-Arab.dic`, **946,155 bytes**. Nothing was missing; the path was assembled rather than asked for, and `tests/test_waw.py` already did it right with `Path(klpt.__file__).parent`. With that fixed it **still exited 1**, on `UnicodeEncodeError`: a script gets cp1252 stdout on Windows, so the summary line went out and the Kurdish word pairs — *the finding* — did not. That is the worse of the two, being **exit 1 with the headline already printed**, which reads as success to anything checking the first line. `cli.use_utf8_streams` calls itself “the first statement of every `main()`” and all six parsers call it; this script never did. **The numbers were right.** With both fixed, exit 0, and every figure reproduces four days on — 24894 items, 0.84% altered, 24051 distinct raw forms to 24000 normalized, **0.21%** would have failed to match, `heh_doachashmee=204` — as do all six word pairs the evidence file quotes. So the fix is to the reproduce path, not the number; recorded because a row resting on a measurement nobody can re-run rests on judgment whatever the number is. Three tests now run the script as a **subprocess** (an import reaches neither the module-level path nor stdout's encoding), require exit 0, and compare the evidence file against what came out, with the figures **parsed from that file** rather than written into the test. The control: `0.21%` and `0.84%` both appear in the document, so the collision rate is asserted in its own table row and the altered-items rate asserted absent from it. **7/7 mutations**, after a 7/7 that was not trustworthy — both restored defects had orphaned an import (F401, D-148/D-150's contamination), and the sweep's own lint check was over-broad: `verify.sh` lints `src tests` and mypy declares `files = [“src”, “tests”]`, so **`scripts/` is outside the gate's lint and typecheck entirely** — named, not fixed here. `evidence/m0-15s-reproduce-command-could-not-run.md`. |
| M0.16 | Download the interim audio corpus | BLOCKED | `BLOCKED.md` #1 — the route itself is gone, not merely unreachable. #6 tracked whether the hosts answered, and from here they do; the corpus does not. Common Voice moved to Mozilla Data Collective in October 2025 (account + accepted terms, which is Hawa's to click, not mine); OpenSLR has **0 Kurdish resources of 156**; `facebook/omnilingual-asr-corpus` is CC-BY-4.0 and ungated but ships **349 configs with no `ckb`**; the two ungated ckb candidates that remain have **no licence** — a D-002 hard reject — and one of them has no audio column at all. Measured, see `BLOCKED.md` #1 §"Option 3 closed". I marked this TODO earlier today on the strength of the hosts answering 200; that was true and it was the wrong question. The importer (M0.14) is built, tested, and has nothing to import. |

**M0 cannot be closed while M0.12/M0.13 are blocked.** The harness is buildable and testable
without the audio; the *measurement* — which is what M0 exists to produce, and what every
downstream threshold depends on — is not. See `BLOCKED.md`.

## M1 — task ledger

| Task | Definition of Done | Status | Evidence |
|---|---|---|---|
| M1.9 | Installed-wheel FFmpeg verification and provisioning | DONE | `src/hawedit/ffmpeg_setup.py` + `tests/test_ffmpeg_setup.py` + `scripts/fetch-ffmpeg.sh`. The wheel authenticates the provisioner through its RECORD, Linux provisions the pinned receipted generation in per-user state, Windows/macOS return bounded package-manager remediation, and `--check` never downloads. A clean no-checkout wheel install passed its hash lock, `pip check`, script authentication and real RTL probe. `evidence/installed-ffmpeg-provisioning.md`, D-150. |
| M1.1 | §4.2 Viterbi forced alignment on CTC emissions, in-house per §7 | DONE | `src/hawedit/forced_alignment.py` + `tests/test_forced_alignment.py`. Monotone non-overlapping spans, every token framed, infeasible input refused rather than guessed. No new dependency. **Amended 2026-08-09 (D-079):** "infeasible input refused rather than guessed" was **untested** for the emissions case — the three checks implementing it could be deleted together with the suite still at `exit=0, 0 FAILED`, while `viterbi_align` degraded from a documented `AlignmentInfeasible` to a bare `KeyError: 0`, in the module whose spans feed §5's anchors and every caption time. Now pinned by type *and* message, alongside the two other untested refusals (`frame_duration_ms <= 0`, a word carrying no tokens), each with a control so a refusal test cannot pass for a function that rejects everything. Guards caught by the files this row cites: **5/11 → 7/11**; chain removal flips the suite to `exit=1, 1 FAILED`. The remaining three are mutually redundant — each covered by the others, so no single-link mutation is observable, and that is redundancy rather than exposure; the adversarial pass's "five unprotected guards here" was right in count and threefold overstated in substance. The stale `(22 tests)` count is dropped rather than restated, since nothing guards per-row counts. `evidence/alignment-refusals-untested.md`. |
| M1.2 | §4.2 sentence segmentation (Kurdish punctuation **plus** VAD pauses) + §5 anchors | PARTIAL | `src/hawedit/sentences.py` + `tests/test_sentences.py`. The punctuation half and the **word-gap** pause path both work — the latter on wholly unpunctuated input; `anchors_for` returns `None` rather than a guess when nothing is complete. Threshold: D-014 — **and as of 2026-08-09 (D-098) that reference is enforced**: every pause test passed `pause_ms=DEFAULT_PAUSE_MS`, so they followed the constant wherever it went and 500→800 left all 1,170 tests green. The recorded value is now checked against D-014 and pinned behaviourally at its boundary (500 ms splits, 499 ms does not). **Demoted from DONE 2026-08-09 (D-081), found by the second adversarial pass.** **Shortfall (one, named):** §4.2's **VAD-pause** half has never fired and cannot. `pause_follows` reaches its VAD branch only when the word gap is *below* `pause_ms`, and that branch then requires a silence *contained* in the inter-word interval whose own length is *at least* `pause_ms` — which forces the gap to be at least `pause_ms`. Both cannot hold. Brute-forced over 3,528 candidate silences: **0 splits**, including a 400 ms silence starting exactly at the first word's end. The runner computes these silences from Stage 0's real Silero output (`_pauses_between`) and hands them to a parameter with no effect — computed and discarded, the same shape as D-070's `natural_silence_ms`. Not fixed here: the containment test is clearly wrong, but what replaces it decides where Kurdish sentences end (and therefore §5's anchors, every boundary and every clip), and there is no labelled Sorani audio to measure the candidates against — `BLOCKED.md` #14 records both options and their opposite failure modes. `tests/test_sentences.py::test_vad_pauses_currently_cannot_split_a_sentence` pins the defect so the dead branch cannot be read as a working feature. `evidence/vad-pause-segmentation-dead.md`. |
| M1.3 | Stage 0 ingest: ffmpeg demux, PySceneDetect, Silero VAD, diarization | PARTIAL | `src/hawedit/ingest.py` + `tests/test_ingest.py` run against real media: `tests/fixtures/kurdish-speech-3cuts.mp4`, built from three 1.4 s segments so the cuts are known. Measured: shot detection on the **source** finds both cuts with **0 ms** error; on the 1 fps proxy it finds **none** (D-023). VAD returns the two utterances, returns nothing on silence, and every segment of a 62 s file stays under the 38 s ceiling. **Progressed 2026-08-10 (D-162):** pass #19 measured a repeated real 38-minute run spending **100.2 seconds (66% of first-run Stage 0)** recreating `audio.wav` and `proxy.mp4`. Reuse now requires matching source SHA-256, ffmpeg command and output size. Extraction is serialized, written to a private suffix-preserving sibling, checked for missing output and mid-run source mutation, then atomically published with fsync'd provenance; a failed rerun preserves the last good artifact. **Shortfall unchanged:** diarization is not run — `IngestResult.diarization` is `None`, never `[]` — pending the gated Community-1 repo (`BLOCKED.md` #4). |
| M1.6 | Model provisioning: readiness report + registry-driven fetcher | DONE | `src/hawedit/models.py` + `tests/test_models.py` + `scripts/fetch-models.sh`. `python -m hawedit.models` reports all 15 §7 components. Sources §7 leaves open are refused, not guessed (D-022). **Corrected 2026-08-09 (D-100):** the *report* was unchecked. It could print every component as `OK` with six missing, invert every verdict, or claim `15/15 available`, and all 1,175 tests stayed green — the two tests touching it asserted `"available" in report`, which the summary line satisfies whatever the marks say. The verdict is now asserted on each component's own line and the summary against the counts it summarises. A second defect fell out of it: `if status.size_bytes` treated a **measured zero** as unmeasured, so a directory of empty files printed no size at all. 7/7 mutations. `evidence/readiness-report-could-print-the-opposite.md`. **Amended 2026-08-09 (D-073):** the fetcher called `snapshot_download` with **no `revision=`**, so it resolved whatever a branch head pointed at on the day it ran — and measured, nothing on disk or in the tree recorded which revision produced the 27 GB of weights already here, making every number in `evidence/m5-*.md` and `m6-3-grounding.md` a measurement against unidentifiable weights. `models/revisions.json` now pins every downloadable repository and `revision_for` refuses an unpinned one exactly as `source_for` refuses an unconfigured id. The pins are measured, not chosen: read live from `HfApi().model_info(repo).sha`, then verified against the weights on disk by comparing the git blob id of each local `config.json` with the Hub's — 4/4 MATCH, so the file names the revision that produced the existing evidence. **Corrected 2026-08-09 (D-127), by adversarial pass #14:** this cell said *all five* repositories and called `pyannote/…community-1` deliberately unpinned with *a test asserts it is the only one*. D-075 pinned it — the repo is gated for downloads and public for metadata, so its revision was always a verifiable fact here — and `tests/test_models.py` now asserts `unpinned == []` with no exemptions. Measured: **6 pinned, 6 registry entries with a download source, 0 unpinned**. D-120 corrected the same two sentences in `AUDIT_REPORT.md` and did not look here, which is the correction-lands-in-one-document failure this project keeps finding; `tests/test_claims.py` now binds the count to `revisions.json` so the next one fails the gate instead of waiting to be read. The tests `exec` the fetcher's real download block against a stubbed Hub rather than grepping the script, because an assertion about a command's text is not an assertion about what it does (D-067). 6/6 mutations caught, two of them in `fetch-models.sh`. `evidence/model-revision-pinning.md`. **Corrected 2026-08-09 (D-127):** this cell ended *Still unpinned: `fetch-ffmpeg.sh` downloads a mutable `main/` archive and executes it with no SHA-256 check* — true when written, closed by D-121. Measured: the URL carries a 40-hex commit, a 64-hex digest is recorded, and it is compared **before** the unzip. A claims test now keys that sentence to the script's own URL line, in both directions. **Audited by pass #14: 7/7** — D-100's report fix, `revision_for`'s refusal and D-099's loader check all held, and no production code changed. `evidence/adversarial-pass-14-2026-08-09.md`. **Corrected 2026-08-09 (D-149):** this row still claimed `fetch-ffmpeg.sh` used mutable `main/` bytes with no checksum after the script had already moved to a commit URL plus the Git-LFS object SHA-256. The stronger remaining defect was local publication: predictable `linux.zip`/`extract` names, no transaction, no installed-byte receipt, and an executable-but-corrupt `.ffmpeg/ffmpeg` that every rerun skipped before failing verification. The Linux fetch now stages under an owner-controlled root, authenticates the archive, verifies exactly one executable pair, records both binary hashes in an immutable generation, and exposes exact launchers with ffmpeg last as the commit marker. Every reuse rechecks the receipt and hashes; drift triggers repair. Executable regressions cover corrupt repair, staged refusal, behavior-preserving byte drift, hardlink victim preservation, linked/non-directory roots, and concurrent/stale locks. `evidence/ffmpeg-provisioning-transaction.md`. |
| M1.7 | §4.1 conjunctive `و` separation (the collision KLPT does not cover) | DONE | `normalize.separate_conjunctive_waw` + `tests/test_waw.py`. Rule: split `و`+R only if R is a valid Sorani word **and** `و`+R is not — a refusal, not a prediction. Measured over all 24,894 dictionary entries (`evidence/waw-separation.md`, D-026): **0 words damaged**, 98.91% of joined forms recovered, 19 `و`-initial words permanently unsplittable because they are words themselves. The 1.09% shortfall has one cause — bare medial `ه` U+0647 — which is D-013's finding seen through a second instrument. **Amended 2026-08-09 (D-089), found by the second and third adversarial passes:** `evidence/waw-separation.md` recorded `waw_initial_words: 491`; the lexicon has **504**, and the file's own constructible count proved it (24 894 − 504 = 24 390, versus 24 403 for 491). It survived two passes because the guard **recomputed nothing** — it bounded three of seven numbers and measured none. Both passes also got the true value wrong, one reporting "504 with duplicates, 492 distinct" where there are 504 with zero duplicates. The test now recomputes every figure from KLPT's lexicon and asserts the arithmetic closes; `constructible_joined_forms` was promoted from prose into the recorded JSON, since the number that disproved 491 lived only in a sentence. Two further copies of Hunspell's stale header count (24,888) were corrected to the measured 24,894 in `tests/test_waw.py` and `normalize.py`. `lexicon_entries: 24894` was right throughout. |
| M1.4 | Stage 1 speech: canonical LLM-7B + CTC-3B/Viterbi with validator escalation | DONE | `src/hawedit/asr.py`, `src/hawedit/asr_worker.py`, `src/hawedit/wsl_setup.py`, `src/hawedit/wsl_asr_locks.py`, and `src/hawedit/models.py`. Segment failures remain visible, per-segment confidence reaches the artifact, the validator is loader- and byte-bound, and an exact CPython 3.12.0 WSL generation with 140 distributions, all 43.5 GB of canonical assets and two CUDA devices passed the live dependency/VEX gate. **Accepted 2026-08-10:** the exact source-bound generation transcribed a representative 2,313.8-second Sorani episode in one 547-region run: 5,897 timed words, two honest unaligned regions totalling 664 ms, two rejected validator corrections whose canonical segments were retained, exact rzgar provenance, worker/published artifact equality and a matching raw SHA-256 sidecar. Labelled CER/WER remains M0.13's external benchmark, not a shortfall in executable Stage 1. `evidence/full-sorani-stage1-acceptance-2026-08-10.md`, D-176. |
| M1.5 | Escalation rule: bottom log-prob quartile + LLM/CTC disagreement (§3 Stage 1) | DONE | `src/hawedit/escalation.py` + `tests/test_escalation.py`. §3's "never escalate on duration or word-count" prohibition is asserted directly; threshold: D-015. **Audited 2026-08-09 by adversarial pass #10 (D-122):** eight of eleven mechanisms initially held. The three survivors were the asymmetric CER argument order, the inclusive `>= 0.15` boundary, and one model producing nothing. Discriminating fixtures now pin all three, including a both-silent control and a decision-level wiring test: 11/11. The earlier audit's caller shortfall is now void: canonical Stage 1 decodes each CTC hypothesis, constructs `SegmentScore`s, calls `select_for_validation`, and routes selected segments through the byte-bound validator while preserving failures and provenance. `evidence/adversarial-pass-10-2026-08-09.md`. **Audited 2026-08-11 (D-170) — the aligner had no independent oracle.** All 33 tests here were hand-written expectations, so a systematic misreading of CTC would sit in the code and in the numbers beside it alike. **Correct, measured twice before anything was touched:** 354 randomized matrices against **torchaudio's** reference kernel, **0 mismatches**; 259 against an exhaustive search over every legal CTC path, **0 disagreements** from either. **What was unheld:** deleting `extended[state] != extended[state - 2]` — this module's own *“the CTC rule that makes this a real algorithm rather than an argmax”* — left all 33 green, **including `test_a_repeated_token_is_separated_by_a_blank`, the test named after it**, whose matrix has the optimal path crossing that blank either way. Now held by an exhaustive-search oracle with a strict-subset control, carrying **no new dependency**: torchaudio is in the hash-pinned lock but `pyproject.toml` avoids it deliberately and it arrives only transitively. **4/5 mutations, 1 by the oracle alone**; the survivor is a no-op of mine and is proved so — blanks sit at even indices, so the deleted `!= blank_id` clause excludes nothing the repeat clause does not (**29 transitions examined, 0 deciding**), which makes it documentation rather than a control. A corpus change chased on a wrong hypothesis is kept on a different, true argument and recorded as having changed no result. No production code changed. `evidence/an-aligner-with-no-independent-oracle.md`. **Audited 2026-08-11 (D-173, adversarial pass 28):** `test_the_stage_0_constants_are_the_blueprints` asserted `== 16_000`, the loudnorm string and `== (1, 720, 28)` — **literals**, with `BLUEPRINT.md` in its own title and never opened — and it covered **four of the six** Stage 0 constants. Measured: `MAX_SPEECH_DURATION_S` **38 → 30 left the whole suite green**, because the only test touching it asserts the margin `OMNIASR_CEILING_S - MAX_SPEECH_DURATION_S >= 2.0`, which 30 satisfies. §3 states `max_speech_duration_s=38` verbatim, and that number decides where Silero cuts every piece of audio handed to ASR. The test now parses §3 Stage 0's two ffmpeg commands and its VAD line for **all seven** values; the margin test stays, as a separate property. `CONTENT_DETECTOR_THRESHOLD` is deliberately **not** bound — §3 writes *“threshold ~27, tuned per content type”* and pinning a tilde as exact would invent precision the frozen document declined to give. **7/7 mutations lint-clean**, both drift directions. No production code changed. `evidence/adversarial-pass-28-constants-the-blueprint-states.md`. **Corrected 2026-08-11 (D-174):** the call order in `normalize_sorani` was right and the reason recorded for it was **not the operative one**. The comment blamed a spelling mismatch; measured, the lookup never receives the word — ZWNJ is U+200C, a format character, so `_TOKEN`'s `\\w+` fragments `وکتێبه‌کان` into **two tokens** before any lookup happens. The distinction is load-bearing: the repair that comment invites — normalising the remainder inside the lookup — changes **nothing**, so it would have been made with no test failing and the order believed free. What the order is worth, across KLPT's 24,894 entries: **11,896** are in this class and of the first 400 the shipped order separates **400**, the reverse **3**. Comment corrected and the tokenization pinned. **2/3 mutations lint-clean** — the ZWNJ-absorbing token pattern is caught by the new test **and nothing else**; the survivor is a demonstrated no-op that confirms the mechanism. **Two errors of mine are recorded rather than tidied away:** the sweep's reversal “survivor” was a bad mutation (it added a pass instead of reordering — the property was already held), and the control written on that false premise was **measured vacuous and removed** rather than kept. No behaviour changed. `evidence/the-ordering-comment-named-the-wrong-mechanism.md`. |

> **M1.3 / M2.7 amendment (D-240, 2026-08-15).** The previously dead runtime seam now exists:
> an injected diarizer attaches only strict, media-bounded exclusive turns; absence/failure is a
> separate structured stage that preserves base ingest; persisted JSON is held to the same schema;
> and Stage 5 consumes only the turns containing the selected anchor edges. Overall completeness
> now requires measured diarization. This is composition evidence, not the missing benchmark or
> production adapter: Community-1 access/bytes, pyannote target locks, attribution, Kurdish DER,
> speaker-face association and real crop review remain open. `src/hawedit/ingest.py`,
> `src/hawedit/diarization.py`, `src/hawedit/pipeline.py`,
> `evidence/stage0-diarization-boundary-fusion.md`.

## M2 — task ledger

| Task | Definition of Done | Status | Evidence |
|---|---|---|---|
| M2.11 | §5 rejection outcomes are produced with reason and discovery path | DONE | `src/hawedit/pipeline.py` + `tests/test_pipeline.py`. Once a survivor is chosen, every ruled-out candidate is serialized once as `RejectedCandidate`; reasons reuse the selector's eligibility/containment computation and `rejected_by_path` reports explicit zeroes for paths that lost none. A run that chose nothing rejects nothing. `evidence/the-rejection-set-had-no-producer.md`, D-153. |
| M2.10 | Every installed CLI writes Sorani reports as UTF-8 when redirected | DONE | `src/hawedit/cli.py` + `tests/test_cli.py`. Every declared entry point configures both streams before its first write; the tests force cp1252 and assert exact UTF-8 bytes, so Windows redirection cannot erase or silently transcode a completed report. `evidence/the-report-died-on-the-way-to-the-file.md`, D-152. |
| M2.1 | §2 text index: BM25 + character 3-grams over normalized Sorani | PARTIAL | `src/hawedit/index.py` + `tests/test_index.py`. The clitic-attachment failure §2 describes is measured: word BM25 scores the stem query **0.0**, n-grams retrieve it. Invariant #3 enforced at the index boundary. Weighting: D-016. **Corrected 2026-08-10 (D-164):** the runner built one document for the whole episode, so BM25 had no passages to rank and its only hit spanned the entire media. Measured on the 38-minute transcript: **1 → 186 documents**, **1 → 37 distinct IDF values**, and the widest sentence window is **4.43%** rather than 100% of the episode. The runner now indexes sentences and the report binds document count to sentence count; nonpositive search limits are refused. **Shortfall:** `Bm25Index.search` still has no production caller because §3 Path A says full normalized transcript while M2 says `transcript → BM25 → Gemini`; product intent is `BLOCKED.md` #18. |
| M2.2 | §5 clip contract + §3 Stage 5 boundary fusion + Kurdish invariant #2 | DONE | `src/hawedit/boundary.py` + `src/hawedit/clip.py`. Invariant #2 is swept over **78,125** combinations — every sign of all seven optional `BoundaryInputs` fields — and enforced again at an explicit render gate. Contract choices: D-017. **Amended 2026-08-09 (D-078), found by the adversarial pass:** this cell said "exhaustively over 3,125 soft-input combinations", and that sweep covered five of the seven fields, omitting `media_duration_ms` and `natural_silence_ms` — the latter wired into the runner by D-070 without extending the sweep this row cites. Now 46,875 boundaries built and 31,250 refused (the two duration offsets that place the media end before `anchor_out`, refused by design because clamping an anchor that does not fit would violate the invariant), 0 violations, 0.40 s. The test asserts those counts, so a field dropped from the product is caught. **What the sweep is worth, measured:** four invariant-breaking mutations are caught with *or* without it, so it is defence-in-depth and never the sole catcher — kept as cheap cover on a non-negotiable invariant, but this row no longer rests on the word. `evidence/exhaustive-sweep.md`. |
| M2.5 | §3 Stage 3 dual-path candidate merge — "union, never intersect" | DONE | `src/hawedit/discovery.py` + `tests/test_discovery.py`. Nothing is dropped (property test over 200 generated inputs); per-path attribution survives so §8.2's `recall_at_k_by_path` and `path_unique_wins` still mean something — **and as of 2026-08-09 (D-101) that survival is checked in the artifact**: hardcoding `DiscoveryPath.VERBAL.value` in `Clip.to_dict` left five test files green, because the shared `a_clip()` fixture is verbal, so both the shape and round-trip tests compared "verbal" against "verbal". Now parametrized over every member on both emitting sites, with a control that three members render as three distinct strings; overlap does not chain; a path never dedupes itself; spans are the anchor's, never widened — §3 Stage 5 owns boundaries. Refuses to invent a cross-path score. D-029. Built ahead of both producers (`BLOCKED.md` #2, #3). **Audited 2026-08-09 by adversarial pass #11 (D-124):** six of ten mechanisms held — union-never-intersect, the anchor's span rather than the union, one claim per visual candidate, no self-deduplication within a path, no cross-media merging, `None` rather than `0.0` for an unmeasured score, and SV6D survival. **Three survivors, all of them fixtures that could not tell two behaviours apart, and no production code was wrong.** The claim-order test asserts *the lower-ranked verbal candidate should claim it* with a fixture where `v1` is rank 1 — id order and rank order agree, so sorting by id alone passed; the new test makes them disagree (`v2` at rank 1). The determinism test shuffles inputs against a reference but leaves no leftovers, so the merge's internal anchor order is already stable and deleting the promised `(media, in_ms, id)` sort was free; pinned now with a visual-only candidate that *starts before* the first anchor, which leftover-append would place last. And `to_retrieved`'s `min` — §8.2's *earliest position at which either path surfaced it* — could become `max`, because the one test exercising it scores at `k=20`; pinned at verbal 9 / visual 2 with a control at verbal 2 / visual 7. 10/10 after. `evidence/adversarial-pass-11-2026-08-09.md`. |
| M2.6 | §3 Stage 4 editorial judge contract: routing interface, verdict, cost model, shadow rule | DONE | `src/hawedit/judge.py` + `tests/test_judge.py`. The shadow is refused at `route()` and again at `to_editorial()` and again at §5's `Editorial`; promotion needs a clear win on ≥20 real items, never a tie or an empty set, and `decide_judge` takes no date argument by design; the 200K tier ceiling is enforced as arithmetic against §3's own 360K with-video figure; a survivor Path A already scored cannot be re-sent for discovery. Kurdish title/description/hashtags are refused if they contain no Kurdish script. D-030, D-031. The call itself is `BLOCKED.md` #3 — and `src/hawedit/smoke.py` is the one place that spends money against it: an opt-in `python -m hawedit.smoke --video sample.mp4` that runs Path A then Stage 4 with real keyframes (M2.9) and announces the cost before spending it. It exists because every other Gemini test uses an injected transport, which can only confirm what the mock was written to believe; the questions it answers — is `gemini-2.5-pro` enabled on this key's project, does the structured-output schema survive a real response, does the model actually answer in Kurdish — have **never been answered**, because there is no key here. Named in this cell as of D-069; it had no ledger entry at all. |
| M2.7 | End-to-end runner: one command over §3, reporting every stage it could not run | DONE | `src/hawedit/pipeline.py` + `tests/test_pipeline.py`. `python -m hawedit.pipeline VIDEO.mp4` runs Stage 0 on real media and exits **1** naming the four blocked stages. Given a transcript and a verdict as stand-ins it runs six stages end to end — ingest → §4.1 → §2 index → §4.2 segmentation → Stage 5 fusion → Stage 6 render — with Stage 5 fusing against the shot cuts Stage 0 found *on that video*. A skipped stage is a `StageSkipped` naming its blocker, never an empty result. D-032. **Amended 2026-08-09 (D-070):** the runner was joining Stage 5 to **three of §3's five out-point signals**. `fuse_boundary` has always had a `natural_silence` branch and this runner never set it, so the branch was unreachable and every clip's out point was decided by tail / shot cut / TimeLens alone — while `_pauses_between` derived exactly that measurement 150 lines earlier, spent it on §4.2's segmentation and dropped it. Natural silence is now the end of the VAD speech region containing `anchor_out`, the mirror of `_vad_onset_for_anchor`; deliberately **not** the next speech onset, which reaches across the whole pause into the following utterance. No threshold is invented — every value is a measured region edge, so it cannot run into the next region. Proved on the artifact through a real run over real media: with the stock transcript the 200 ms tail wins at 1900 ms over the 1790 ms silence and the out point does not move (asserted as the control, and it is the control that discriminates — the rejected reading gives 1954 ms and passes the positive test), and with the last word ending 200 ms earlier the out point lands on 1790 ms with `out_extended_by == "natural_silence"`. Silero also reports speech to 4180 ms on a 4162 ms file, so `media_duration_ms`'s clamp is load-bearing for a second reason. 5/5 mutations caught, one of them in `boundary.py` rather than here. `evidence/stage-5-natural-silence.md`. **Still three of four advisory out-point signals:** `speaker_turn_end` needs the gated diarization model (`BLOCKED.md` #4, 401 from here) and is absent for a stated reason rather than by omission. **Amended 2026-08-09 (D-071):** the overwrite refusal fired **after** the billed Stage 4 `generateContent` and after Stage 2/3 had loaded Qwen and VideoChat3 — so a re-run into a used work directory paid Google and spent GPU time before raising `FileExistsError` with nothing to show. The condition needs only `work_dir`, the media id and the selection. `_assert_no_existing_artifacts` is now called where the selection first becomes knowable — after `_prepare_selection`, again after `--auto-select` settles one (it cannot know until Stage 3 has ranked), and still before the first write — and `clip_id` plus the five artifact paths derive from one place each, so the guard cannot check different files than the run writes. The tests assert the billed call **count is zero**, because the artifact of this fix is a request that never happened; the control requires a clean directory to reach the judge exactly once, which is what distinguishes the fix from a guard that refuses everything. 4/5 mutations caught by those tests and 5/5 by the full gate — the survivor moves guard and writer together by design, and is caught by `test_distinct_selections_do_not_overwrite_each_others_deliveries`. `evidence/billed-before-refusing.md`. **Amended 2026-08-09 (D-072):** the delivery set is now all-or-none, and the shortfall named above turned out to be reachable on the commonest professional frame rate rather than on a crash. The runner wrote the editing JSON, then the SRT, then *built* the EDL — which legitimately refuses SMPTE drop-frame at 29.97 fps — so a real NTSC transcode produced a complete playable 1080×1920 captioned MP4 plus ASS, JSON and SRT with **no EDL**, stage reported skipped and `complete=False`, while the files on disk looked whole. All three are built before any is written, and the `except` unlinks all three (now also catching `OSError`) so a write failing partway is all-or-none too. The MP4 and ASS are deliberately kept — Stage 6 succeeded and `run.render` reports that path, so deleting it would make the report a lie; a mutation that deletes them is caught. 4/4 mutations, after the audit correctly reported the original defect as SURVIVED: cleanup alone fixes the final disk state, so a test that watches every `Path.write_text` and asserts no sidecar write is *attempted* is what makes the build-first ordering load-bearing. `evidence/partial-delivery-set.md`. **Still open:** an NTSC source produces no EDL at all — correct, since drop-frame is refused rather than faked, but writing real drop-frame timecode is unbuilt. **Corrected 2026-08-09 (D-145):** D-103's record reached `transcript.raw.json` and not the run report, whose `transcript` section is the **normalized** transcript and carries no such field. Measured on the real 38-minute run: the raw named 2 regions totalling **664 ms** of speech with no transcription, and the emitted report mentioned neither `unaligned` nor `segment_confidence` — in a module whose docstring opens "§1: fail visible, not silent". `PipelineRun.transcript_gaps` now carries them and the report totals them as `speech_without_transcription_ms`, with the empty case reported rather than omitted so absence is readable. Rejected making `complete` false for dropped speech: that changes what the CLI's exit code means for automation, so it is named for Hawa rather than taken. 5/5 mutations. `evidence/the-report-did-not-say-what-the-transcript-omits.md`. **Corrected 2026-08-09 (D-146):** the row's own promise — every stage yields a result or a named skip — did not hold for a stage that *ran*. `discovery` and `editorial` are typed `StageSkipped \| None` and success was written as `None`, so the real 38-minute run reported `discovery: null` alongside **7** merged candidates, with discovery absent from `skipped` too; only another key disambiguated. A discovery that ran now reports `{skipped: false, candidates: N, by_path: …}`, derived from the candidates rather than a second flag that could disagree, with the per-path split included because §8.2 partitions on it. An explicit skip still wins, and `None` still means nothing is known. 5/5 mutations, with two controls pulling in opposite directions. `evidence/a-stage-that-ran-reported-nothing.md`. |
| M2.8 | Gemini credential panel + the real §3 Stage 4 judge | DONE | `src/hawedit/credentials.py` + `src/hawedit/gemini.py`. `python -m hawedit.credentials` verifies a key against Google before storing it, refuses any target git tracks, and never prints it. `GeminiJudge` implements `EditorialJudge` with schema-enforced output, real `countTokens` before the billed call, temperature 0, bounded retries on transient failures only, and §3's ZDR gate as a required value. Every check in `JudgeVerdict` applies to model output. D-034, D-035. **Hardened 2026-08-09 (D-148):** `test_writing_to_a_tracked_path_is_refused` aimed a real credential writer at `Path(__file__)`, making `assert_ignored_by_git` — the thing under test — the only barrier between the suite and its own source. An audit that neutered that guard for one run proved it: 262 lines of this file became eleven `KEY=VALUE` fragments under the header "hawedit credentials. Git-ignored. Never commit this file." Restored from HEAD; nothing reached a commit. The target is now a path that does not exist and is not ignored — `git check-ignore` answers from patterns, not the filesystem — so the refusal is identical and a failure costs one stray file instead of a deleted test, with assertions bracketing the call so a refusal arriving *after* the write is caught too. The guard itself is sound and fails closed, including when git is missing. `evidence/a-test-that-could-delete-itself.md`. **Audited 2026-08-10 by adversarial pass #19 (D-134): 7/7, and the headline claim survives while the runner's index shape did not.** The `0.0` claim reproduces exactly — on the clitic pair, word BM25 scores the stem query **0.000000** and the n-gram field **1.829909**. But `pipeline.py` built `from_transcript` — **one document for the whole episode** — three lines before it computed the sentences. Measured on the real 38-minute transcript: **1** document, and BM25's idf at N=1 is `log(1 + 0.5/1.5) = 0.287682` for **every one of 2,784 terms** — one distinct value against the per-sentence index's **37** over 0.855..4.826 — so rarity is invisible, length normalization compares the document to itself, and every query returned **1 hit whose window was 322..2,313,729 ms, the whole 38.6 minutes**. §3 Stage 5 consumes a window. `from_sentences` — whose own docstring says it is “what lets a hit hand Stage 5 a real time window instead of a whole episode“ — existed and had no caller. The runner now builds it: **186 documents**, 37 idf values, and the widest window any hit can hand Stage 5 is **102,524 ms of 2,313,729 — 4.43%** instead of 100%. Invariant #3 moved with it rather than being left behind: `from_sentences` took a bare `media_id: str`, which cannot be refused, and now takes the `NormalizedTranscript` so `assert_model_input` guards the path the runner uses. **`test_the_run_report_serializes_to_json` asserted `document_count == 1`** — the defect written down as an expectation — proved wrong by the measurement above before being changed, and now asserts the sentence count *and* that it exceeds one. **Second finding: D-090's sibling.** That decision fixed `scored[:k]` in `visual_index.retrieve` for negative `k`; `search` ended in `hits[:limit]` and kept it — measured on a 10-document index, `limit=-1` returned **9** hits (the best nine, silently a different operation) and `limit=-10` returned **0**. Refused as arithmetic, with `limit=1` as the control the over-strict mutation trips against. **Two of my own claims were wrong and the tests caught them:** a single-document index does *not* score rare and common terms equally (0.287682 vs 0.395563 — term frequency still varies), so both tests and both source comments now state what one document actually cannot do. **Still open:** `Bm25Index.search` has **no production caller** — the index is built, reported and never queried, because §3 Path A sends the full transcript “not a filtered subset“ while §9's M2 row describes “transcript → BM25 → Gemini“. `BLOCKED.md` #18 records the disagreement for Hawa rather than inventing a query. `evidence/adversarial-pass-19-2026-08-10.md`. **Audited 2026-08-11 (D-173, adversarial pass 28):** `DEFAULT_NGRAM_SIZE` **3 → 4 left the whole suite green** — bound to the blueprint by nothing and pinned behaviourally by nothing, though §2 states *“BM25 + character 3-grams”* and singles the choice out (*“Character n-grams matter more than usual — Sorani is morphologically rich with heavy clitic attachment”*). Now read from the frozen document. **And a document binding proves the number, not that anything consults it:** a second test asserts on the artifact — the emitted n-grams of a word long enough to distinguish 3 from 4 — and it is the only test that catches `character_ngrams` hard-coding a size. **7/7 mutations lint-clean.** No production code changed. `evidence/adversarial-pass-28-constants-the-blueprint-states.md`. **Audited 2026-08-11 (D-172) — first pass on this row, 4/6.** Six defects that ship a wrong clip rather than raising. **Survivor one, real:** §3 Stage 5 says *“shot_cut within 400 ms”* and “within 400” includes 400 — changing `<=` to `<` on either edge left the whole suite green, worth **200 ms** of delivered clip at the out edge (`14400 by 'shot_cut'` against `14200 by 'tail'`) and **400 ms** at the in edge, plus the attribution §8.2 reads. **Survivor two, a proved no-op:** `anchor_in` is seeded first, so a cut exactly at it ties and `min` returns the seed — measured `final_in 10000 by None` either way; documented, not counted. **And the larger one:** `test_the_constants_are_the_ones_section_3_stage_5_states` asserted `== 120`, `== 200`, `== 400` against **literals typed into the test** and never opened the frozen document it is named after, so a constant and its literal could move together unnoticed. It now parses §3 Stage 5's SOFT ADJUSTMENT block for all three, with non-vacuity taken from the document's own redundancy — the window is stated once per edge and both must agree. Rejected a test that merely follows the constant (D-098's lesson). **7/7 mutations lint-clean**, both edges, all three constants, both drift directions. The floor moved 1558 → 1561: a duplicate test was merged away and the gate refused the shrink until the edit was made visible, which is what it is for. `evidence/the-window-edge-and-a-test-that-never-opened-the-blueprint.md`. **Hardened 2026-08-12 (D-195):** a guard-revert sweep over every `raise` in `clip.py` — located by AST, deleted one at a time, whole suite each time — found **11 of 15 held**, and the four that were not included **both of the judge's halves of `assert_renderable`**, the gate `render_clip` calls before starting an encoder: `if self.editorial is None` and `if self.output is None`. Its docstring says “§8.3 requires this on every shipped clip”; the two QC refusals above them are held by 11 and 3 tests, and these two by nothing — so a clip the judge never scored (no meaning-fidelity, no misleading-edit score) or one with no title/crop target could have reached ffmpeg if the check were refactored away, suite green. Also unheld: `Output.durations` having to be positive seconds, and `Qc.flags` having to be a tuple of non-empty strings — whose `from_dict` sibling **is** held, so the JSON door was covered and the constructor was not. **No production code changed**; all four refusals were already correct. **4/4 mutations caught lint-clean**, each by exactly one named test and by no gate test, file restored byte-identical. `evidence/the-gate-before-the-encoder-had-two-refusals-nothing-held.md`. **Audited 2026-08-09 by adversarial pass #15 (D-128):** six of nine mechanisms held — the 200K ceiling against §3's own 360K figure, an uncounted request refused rather than assumed small, the don't-pay-twice refusal, the 20-item floor, and the empty-set and below-floor refusals. **Three survivors, and one of them is this cell's own sentence.** *"never a tie"* was unheld: `test_a_shadow_that_merely_ties_is_not_promoted` calls `decide_judge(5, 5)` — **10 items, below the 20-item floor** — so the floor answers and the tie rule is never reached, and its reason assertion looks for `"tie"`, which matches **ties 0** in the header line every decision prints. Two reasons to pass, neither the rule; `shadow_wins <= incumbent_wins` → `<` left the suite green and a tie above the floor would have promoted the shadow. Now pinned at ten against ten, asserting the floor did *not* answer, with a control at eleven against ten that promotes — refusing every tie *and* every win would satisfy the first test and pin the incumbent for ever. Also unheld: a request at **exactly 200,000** tokens (§3 says *under* 200K; `>=` → `>` was free, D-098/D-122's shape a third time) and the **20-keyframe cap**, where inline image bytes are billed. 9/9 after; no production code changed. `evidence/adversarial-pass-15-2026-08-09.md`. **Audited 2026-08-11 (D-178, adversarial pass 29) — the contract's own enforcement had never once refused.** Tracing the whole suite with a stdlib `sys.settrace` line tracer: **438 raise statements in `src/hawedit`, 125 never executed**. The standout is here — `_assert_verdict_matches_request` runs on every judged run, its call site and **both** comparisons execute, and **neither of its two refusals ever fires**. It is the only thing between an adapter's answer and §5's editorial block, and D-177 measured one iteration earlier what a verdict for other footage carries there. Each refusal now has its own test — separately, since an adapter answering the right candidate over the wrong seconds passes the identifier check completely — plus a control requiring an adapter that answers its own request to be accepted. **4/4 mutations lint-clean, three caught by the new guards alone.** Rejected installing `coverage` for the measurement (it would reopen D-139's pinned gate environment) and rejected one test for both refusals (D-177's first version made that mistake). **125 is not claimed as 125 defects** — much is unreachable behind BLOCKED #3, #4 and #16. No production code changed. `evidence/adversarial-pass-29-refusals-nothing-reaches.md`. **Corrected 2026-08-09 (D-115):** the runner destroyed the report of a completed run by the act of capturing it. `python -m hawedit.pipeline … --json > report.json` on hawapc01, after a 38-minute Stage 0 and ~10 minutes on two 3090 Ti with 547 regions transcribed, exited **1 with a 0-byte file** — `UnicodeEncodeError: 'charmap' codec`, because Python takes the streams' encoding from the locale and on Windows that is cp1252 while this product's output is Sorani. Two quieter forms of the same cause: characters *inside* cp1252 are written as cp1252 bytes, so a blocked run's captured stdout is **not valid UTF-8** (9 high bytes, `0xB7` for `·`), and stderr defaults to `backslashreplace`, so `✗` reached the log as the literal `\u2717`. None of it shows on a console, where Python writes UTF-16 to the Windows terminal. `src/hawedit/cli.py`'s `use_utf8_streams()` is now the first statement of all five `main()`s; the same command re-run emits **1,010,979 bytes that decode as UTF-8**, 6,104 words and `speech_without_transcription_ms: 664` intact. `tests/test_cli.py` drives every entry point declared in `pyproject.toml` under `PYTHONIOENCODING=cp1252` and asserts the Sorani sentinel's UTF-8 **bytes** — not that the call is present, which a call after the first write would satisfy — so a sixth entry point is covered the day it is added, and the check still discriminates on the Linux runner where the locale is UTF-8 already. Rejected `__init__.py` (the one file the ledger test exempts, and a library must not reconfigure its importer's streams) and documenting `PYTHONUTF8=1` (correct, and not a fix). 8/8 mutations, five of them removing the call from one `main()` at a time — D-105/D-108/D-112's shape. `evidence/the-report-died-on-the-way-to-the-file.md`. **Progressed 2026-08-09 (D-116):** §5's *"Rejection is a first-class outcome … that set is your only measure of recall"* had a type, validation, `to_dict`/`from_dict` and its own tests, and **zero construction sites in `src/`** — never computed, not computed and discarded. The runner decides which candidate survives in two places and returned only the winner: on the real 38-minute run, Stage 3's **7** candidates became 1 chosen and 6 with no trace. `PipelineRun.rejected` now carries every candidate a decision ruled out, with the reason read off a computation the runner already performed — `_complete_sentences_within` is shared with the selector so the record cannot claim an eligibility the selector disagrees with — and `rejected_by_path` names every path that found a candidate, at zero if it lost none, because §8.2 measures Recall@20 *per discovery path*. The survivor is chosen once rather than again inside Stage 4, so a candidate ruled out twice is not counted twice, and the record exists on a run whose Stage 4 is blocked — every run on this machine until `BLOCKED.md` #3 clears. Nothing is recorded when nothing chose. Rejected recording Stage 2's 634 unretrieved and 43 reranked-and-dropped **windows**: a window has no `discovery_path`, and filing it under one credits a path with a rejection it never made. 6/6 mutations. `evidence/the-rejection-set-had-no-producer.md`. **Audited 2026-08-09 by adversarial pass #16 (D-129): 3/7.** The exit code is pinned in both directions — 0 for an incomplete run CAUGHT, and 2 (the code a *refusal* uses) CAUGHT — and the window plan ignoring Stage 0's cuts is CAUGHT. **Four survivors, with one shared cause:** `PipelineRun.complete` decides the exit code and has eleven conjuncts, and three of them (`not self.skipped()`, `bool(self.visual_windows)`, `bool(self.candidates)`) could each be replaced by `True` with 1,302 tests green — because **`complete` was never once True in the suite**. Measured: even `full_run` is incomplete, lacking `candidates` and carrying `visual_index` and `discovery` skips, so a conjunct and a no-op were indistinguishable. There is now a run where every stage produced something (`complete is True`, `skipped() == ()`), built through the real `run_pipeline` with a discovery producer, a composer and a judge rather than fabricated dataclasses, and each conjunct is removed from *that* run. The fourth survivor: Stage 5 fused against cuts from nowhere on this video with nothing noticing — asserted on the **input** now, because §3 takes the *latest* out-point signal and natural silence is the whole file here (measured: anchor 2000..2500 gives `out_extended_by='natural_silence'`, `final_out=4162`), so no anchor lets the cut decide the outcome. 7/7 after; no production code changed. `evidence/adversarial-pass-16-2026-08-09.md`. **Corrected 2026-08-11 (D-171):** *“reporting every stage it could not run”* rested on a hand-written tuple of nine stages inside `PipelineRun.skipped()`. Measured: deleting `("delivery", self.delivery)` from it left the **whole suite green** — while `visual_index`, `discovery` and `editorial` were each caught, 3/4. On the report a reader is handed, a run with render and delivery skipped then printed *“INCOMPLETE — **1** stage(s) did not run”* instead of two, never named the delivery set, and dropped `blocked_by=('§2 delivery set',)`. **Never an exit-code defect:** `complete` separately requires `isinstance(self.delivery, Delivery)`, so the CLI still exited 1 — what failed was §1's *fail visible, not silent*. `delivery` was the survivor precisely because `complete` covers it directly, while `visual_index` and `discovery` are covered only by proxy fields and so depended on the list. `skipped()` now derives from `fields(self)`; declaration order **is** pipeline order, so the sequence is unchanged and the defect is unconstructible rather than guarded. Rejected a second hand-written list to police the first (four prior drifts: D-127, D-129, D-141, D-149) and rejected enumerating every dataclass field in the test, which mypy refused correctly. **3/3 mutations lint-clean**, with a control that must stay green — the complete hand-written list is behaviourally identical and does, so the tests pin the property and not the fix. Two contaminated runs of mine were discarded, not counted. `evidence/a-skipped-stage-the-report-could-not-name.md`. **Audited 2026-08-11 (D-177) — eleven refusals, 10/11.** The argument refusals are exemplary and `test_every_refusal_in_the_source_has_a_case` is derived from the source, so a new one cannot arrive uncovered — it reads the arg-parsing block, and **the survivor lives outside it**: `run_pipeline`'s check that a *supplied* verdict's span is the selected sentence anchors, deletable with the whole suite green. That matters now because `--verdict` is the **only** Stage 4 route while BLOCKED #3 stands, and `JudgeVerdict.__post_init__` cannot see it — a verdict for another clip satisfies `clip_in_ms <= payoff_at_ms <= clip_out_ms` perfectly. Measured on the §5 block that ships: a verdict scored 900000..904000 ms carries **`payoff_at_ms: 902000`** into a clip running 100..4100 ms, with every editorial score reached on footage the clip does not contain. Each end of the span is now covered separately — the first version moved both at once and the audit showed comparing only the in-point still caught it, leaving the right-start, wrong-end case unheld. **2 of 3 counted mutations caught by this guard alone.** The `judge is None` clause survives and is **not counted**: with discovery driving the run, `anchors` is `None` and that path never reaches the check, so its state is unconstructible — the control I wrote for it measured nothing and was removed. No production code changed. `evidence/a-verdict-for-other-footage.md`. **Corrected 2026-08-12 (D-183), D-111 one representation over.** D-111 fixed `report["discovery"]` reading `null` for a stage that ran; the **printed** report — what the documented invocation produces — still said nothing about Stage 3 at all, while a *skipped* discovery printed its `SKIPPED` line. Measured on the full composed run over the real 38-minute file with the champion adapter: the printed report went straight from Stage 2's `7 reranked survivor(s)` to §4.2's `185 sentence(s)`, while the same run's `--json` carried `discovery: {skipped: false, candidates: 7, by_path: {visual: 7}}` — one of those 7 being the window D-182 recovered. Now prints `stage 3 7 candidate(s) [visual 7] · 0 rejected [visual 0]`, read off `_discovery_ran()` so the two reports cannot disagree, with rejections shown even at zero because §5 calls that set the only measure of recall and the set was computed. **5/5 mutations** — after 3/5, the audit having caught a split test of mine that passed for the wrong answer because the rejection split prints the same path names. `evidence/the-printed-report-never-said-what-stage-3-found.md`. **Hardened 2026-08-09 (D-113):** `test_writing_to_a_tracked_path_is_refused` aimed a real credential writer at `Path(__file__)`, making `assert_ignored_by_git` — the thing under test — the only barrier between the suite and its own source. An audit that neutered that guard for one run proved it: 262 lines of this file became eleven `KEY=VALUE` fragments under the header "hawedit credentials. Git-ignored. Never commit this file." Restored from HEAD; nothing reached a commit. The target is now a path that does not exist and is not ignored — `git check-ignore` answers from patterns, not the filesystem — so the refusal is identical and a failure costs one stray file instead of a deleted test, with assertions bracketing the call so a refusal arriving *after* the write is caught too. The guard itself is sound and fails closed, including when git is missing. `evidence/a-test-that-could-delete-itself.md`. **Audited 2026-08-10 by adversarial pass #20 (D-137): 18/18 after two real gaps were closed, and this cell's leading claim was one of them.** *“verifies a key against Google before storing it”* lives entirely in `main()`, and **nothing drove `main()`** — `grep -n "credentials.main" tests/` found no caller. Two mutations survived: storing a key Google **rejected**, and moving the write **above** the validation. The units either side were thoroughly tested (`validate_gemini_key` on four transports, `write_credential` on symlinks, hardlinks, tracked paths and permissive modes, `mask` on two keys) and the gap was exactly between them. Four tests now drive the panel with the network and the writer replaced by recorders, asserting `order == [“validate”, “write”]` rather than only that a key was stored — which a store-first panel also satisfies — plus a blank-entry control that must reach neither. **Second gap: the TOCTOU half of the O_NOFOLLOW reconstruction.** `_O_NOFOLLOW` is measured **0** on Windows, so the guarantee is two halves — refuse a symlink before the open, prove after it that the handle is the file that was checked — and only the first had a test, on the platform where the second actually runs. The race is now forced by making `os.lstat` answer about a different file, with a truthful-lstat control. **Two further survivors were platform artefacts and were measured, not reported as holes:** `0o600` and `0o666` both yield `0o666` on Windows, and removing `_O_NOFOLLOW` from the open flags is a no-op when it is 0 — both properties are held here by the pre-open refusal and `assert_owner_only`'s ACL read, whose mutations are caught. **And one mutation of mine was wrong:** `if attempt < max` only skips the final sleep, so it never made retries unbounded; the bound is held by a test asserting exactly 3 `generateContent` calls. **The audit's own first run was contaminated by the guard it was auditing** — the git-ignore mutation made the guard fail open, D-113's probe file landed in the repo root, and that test's *first* assertion is that the probe does not exist, so 16 of 20 results were red for the wrong reason and only a manual `rm` could clear it. The probe is now removed in `finally` with a message naming what to inspect; the check itself is unchanged. **Everything in `gemini.py` survived the pass** — schema-enforced output, `countTokens` before the billed call, the tier ceiling, temperature 0, no retry on a 400, the retry ceiling and the ZDR gate all redden when reverted. **The first push of this pass was refused by the gate**, and rightly: the identity test skipped where the kernel has `O_NOFOLLOW`, so the runner reported *1372 passed against a floor of 1373 … a skip condition is creeping*. A guard only Windows exercises is a guard CI never checks, so `_O_NOFOLLOW` is patched to **0** instead and the branch runs on both platforms — 26 tests in the file, no skips, and removing the comparison still reddens. `evidence/adversarial-pass-20-2026-08-10.md`. **Audited 2026-08-11 (D-176) — the output half of “never prints it”, 3/4.** Four escape routes for the key were mutated; the header, the mask and the unreachable-API message are all held. **The survivor: the status readout for an *already stored* key.** The existing `test_the_panel_prints_the_mask_and_never_the_key` is not weak — it drives the **entry** path, where the key comes from `getpass`. `_drive_main` stubs the validator, the writer and `getpass` but **not** `credential_status`, so with no key configured `main` takes the `key is None` branch and the `else` — the branch every user with a stored key sees on every run — executed in no test. `--check` was driven by nothing either. Both now run the real `main()` with a stored key and assert the key is **absent** and the mask **present**, the second being the control against “never prints it” becoming true by silence. **3/4 mutations lint-clean, two caught by the new guards alone** — and the second was unlooked for: `--check` returned 0 whatever the API said, so a script gating on it would proceed with a revoked key. A fourth was format-dirty and is not counted. No production code changed. `evidence/the-status-readout-nothing-drove.md`. **Corrected 2026-08-12 (D-191):** a guard-revert sweep over every `raise` in `credentials.py` — located by AST, deleted one at a time, whole suite each time — found **6 of 10 held**. The one that mattered: `restrict_to_owner` ran *after* the body was written, so a failed narrowing left the plaintext key on disk at inherited permissions while the panel printed “Refusing to leave a credential at inherited permissions” and returned 2. Measured against a **real** `icacls` failure (unresolvable principal, exit 1332) in a directory granting `Everyone:(OI)(CI)F`: 95 bytes containing the key, readable by Everyone — **identically whether the refusal was present or deleted**, so the guard's only contribution was the exception. POSIX was never exposed (`O_CREAT` at `0o600` cannot be widened); on Windows the mode argument carries only the read-only bit, so the whole span between the open and the narrowing was a window. The narrowing now happens above the write *and* above `os.ftruncate`, so a refusal exposes an empty file and leaves the previous key intact. `_IS_WINDOWS` became a module constant because the `icacls` refusal was a branch the Linux runner can never execute — patched, never skipped, the repo still runs 0 skips. **5/5 mutations caught**, file restored byte-identical. `evidence/the-refusal-named-the-state-it-had-already-created.md`. |
| M2.3 | Stage 3 Path A (Gemini reads the full transcript) | DONE | `src/hawedit/path_a.py` + `tests/test_path_a.py`. Sends the **whole** normalized transcript — a test asserts every fragment reaches the judge, because sending a subset is the exact failure §3 built the dual path to prevent and would be invisible in the output. **Amended 2026-08-09 (D-086), found by the third adversarial pass:** that test could not see what it protected. Its three asserted fragments were all entries in the fixture's `words` tuple, so `_timing_table` rendered each of them above the transcript and `fragment in api.prompt` was satisfied by the timing table alone — measured, replacing `text=transcript.text_ckb` with `text=""` left `tests/test_path_a.py` at 21 passed and the **full suite at `exit=0, 0 FAILED`**, with the judge receiving no Kurdish text at all. It now asserts the whole `text_ckb` verbatim, derives the discriminating fragments at runtime as `text_ckb` minus `words` and refuses a fixture where that set is empty, and carries a control requiring a timing row so a prompt dropping timings to keep text cannot pass. 3/3 mutations. Separately, an agent claim that a `RawTranscript` reaches `countTokens` before invariant #3 refuses **did not reproduce**: measured with the suite's recording transport, both `discover` and `build_request` raise with zero endpoints hit and no raw text in any body. `evidence/path-a-transcript-unasserted.md`. Refuses to split a too-long transcript rather than choosing which parts the Kurdish judge reads. Invariant #3 at the door; every returned span checked against the transcript's own range; ranks dense and ordered for §8.2. Wired into `pipeline.py`. D-036. |
| M2.4 | One rendered clip | DONE | `evidence/m2-4-rendered-clip.mp4` (1080×1920, 2.2 s) + `evidence/m2-4-frame.png` — a real vertical clip with Kurdish captions burned in, rendered by `src/hawedit/render.py` + `tests/test_render.py`. Was marked BLOCKED behind `BLOCKED.md` #5 for two days after #5 was resolved; `tests/test_claims.py` now fails on a BLOCKED row whose every blocker is resolved. **Amended 2026-08-09 (D-087), found by the third adversarial pass:** `assert_captions_within_clip` guards Kurdish invariant #4 at the burn and its *function* is well covered, but its **call in `render_clip` was protected only by an import-usage lint rule**. Deleting the call trips ruff (unused import) and the nested-gate tests then fail on a red lint — a linter, not a test. The import-preserving mutation, feeding the guard a synthetic always-valid ASS instead of the file on disk, left ruff clean and the **full suite at 0 failures**, and under it an ASS with source-absolute stamps ships a playable, caption-free MP4 reporting `captions_burned_in=True`. Now pinned through the render path: a file stamped a minute into the episode must raise `CaptionsOutsideClip` and write no MP4, with a control that the ordinary ASS still renders. The import-preserving mutation now fails exactly one test with ruff clean. `evidence/caption-guard-wiring-unprotected.md`. |
| M2.9 | §3 Stage 4's request carries real source pixels: JPEG bytes sampled from exactly the candidate slice | DONE | `src/hawedit/keyframes.py` + `tests/test_keyframes.py`. `extract_judge_frames` cuts up to 20 evenly spaced JPEGs from the candidate span with ffmpeg and hands the judge bytes, not paths and not a description — the failure it exists to prevent is a multimodal request that is silently text-only. Wired into `pipeline.py` behind the judge's own `requires_keyframes`, so a text-only judge is not charged for frames. Measured on the fixture at `0..4162 ms` asking for 6: **6 frames, every one a real JPEG** (`FFD8` start-of-image, 2,624–3,424 bytes), timestamps `347 / 1040 / 1734 / 2428 / 3122 / 3815 ms` — six distinct stamps, all inside the requested span, spaced 693–694 ms apart. Only **3 distinct payloads** across the 6, which is correct rather than a defect: the fixture is three static shots, so the two frames sampled inside one shot are byte-identical pixels. A span with `out_ms <= in_ms`, a count outside 1..20, and a missing ffmpeg are each refused rather than returning an empty tuple that would read as "no frames here". `evidence/unlisted-modules.md`. The *call* that would carry these bytes to Google is `BLOCKED.md` #3, which is M2.6's shortfall, not this row's: the transport is complete and measured. **Audited 2026-08-09 by adversarial pass #13 (D-126):** every measurement in this cell reproduces exactly — 6 frames at **347/1040/1734/2428/3122/3815 ms**, 693–694 ms apart, 2,624–3,424 bytes, all `FFD8`, 3 distinct payloads. **Only four of ten mechanisms were held, the worst of any pass, and this is the row whose artifact is billed.** The serious one: the timestamps are the *request echoed back* — `min(out_ms, round(in_ms + (index + 0.5) * step_ms))` — so replacing `-ss in_ms` with `-ss 0` left them identical and the suite green, meaning the bytes a multimodal judge is charged for could come from anywhere in the media while the request describes the candidate (M3.4's lesson, one module over). Now asserted on the **payloads**: the fixture's three shots hash to `46f2c52ce626999c` / `51f35b218c7a4534` / `d700e83a931dfb52`, so three spans must return three pictures, with a control that the last shot differs from the first specifically. **Three of the four refusals this cell states as verified were untested** — the zero-length and inverted span, the count *floor* (only the ceiling was covered), and a missing or failing ffmpeg returning `()`, which is exactly what a text-only request looks like. This branch's D-107 private per-call namespace is stronger than the upstream stale-file refusal: an 8-frame call followed by a 2-frame call must return exactly 8 then 2, with no private pixels retained; a separate hostile-stale-file test proves caller-owned files never enter the result. And the `requires_keyframes` gate had no test for the attribute being *absent*, so making it unconditional passed and a text-only model would be billed for up to twenty inline images. 10/10 after; no production code changed. `evidence/adversarial-pass-13-2026-08-09.md`. |

> **M2.6 amendment (D-174).** The §7 shadow-routing guard is now held for every concrete Gemini
> judge constructor, including Vertex's separate ADC constructor. The declared constructor map
> must equal the transitive production class hierarchy; every class refuses the shadow and a
> positive control accepts the pinned incumbent. `evidence/confidential-judge-routing-copy.md`.

## Kurdish invariants — where each is enforced

| # | Invariant | Enforced in |
|---|---|---|
| 1 | `transcript.raw.json` never mutated after write | `transcripts.py` — refuse-rewrite, frozen types, SHA-256 tamper evidence |
| 2 | `final_in <= anchor_in` and `final_out >= anchor_out`; `sentence_complete == false ⇒ reject` | `boundary.py` — by construction in `fuse_boundary`, and again at `assert_boundary_invariant` / `Clip.assert_renderable` |
| 3 | Indexes, embeddings and model inputs read `norm`, never raw | `transcripts.py` (types + `assert_model_input`), `index.py` (index boundary) |
| 4 | Captions render `shaping=complex`; build asserts libass has HarfBuzz; golden-image test in CI | `captions.py` — **enforced, and audited 2026-08-08**. Shaping, the stack check on a real build, font coverage on the real font, and a golden render compared per gate run with `shaping=simple` as a failing negative control: all four reproduced, 6/6 mutations caught, and the reference is **pixel-exact** on ffmpeg 8.1.1 with 2734 ink px of real shaped text rather than a blank compared to itself. **The fifth claim in this cell was not true when it was written:** "our own line breaks" had no rendered evidence — the golden text is 28 chars against a 32-char limit, so it is one line and cannot exercise wrapping. Three pixel tests close it (two bands with our `\N`, one without, and a rendered demonstration that `WrapStyle: 2` changes the band count). `evidence/rtl-shaping.md`, `evidence/rtl-shaping-wrapping.md`, D-061. |
| 5 | Word timings from OmniASR CTC Viterbi alignment only | `alignment.py` + `transcripts.py` — refused at construction |

## M3 — task ledger

| Task | Definition of Done | Status | Evidence |
|---|---|---|---|
| M3.11 | SRT preserves §4.3.5's word-aligned RTL line breaks | DONE | `src/hawedit/delivery.py` + `tests/test_delivery.py`. SRT and ASS share the same wrapper and measured width; a real 38-minute transcript had 149 of 182 clip-eligible sentences wider than that width. `evidence/the-srt-let-the-player-choose-the-break-points.md`, D-151. |
| M3.1 | §4.3 caption generation: shaping, stack check, font coverage, own line breaks | DONE | `src/hawedit/captions.py` + `tests/test_captions.py`. Font coverage asserted against the real OFL-1.1 Noto Naskh Arabic shipped in `assets/fonts` — full Kurdish coverage measured (D-018). **Corrected 2026-08-10 (D-163):** the required set omitted `ک` U+06A9 and `ی` U+06CC even though §4.1 normalization produces those exact forms and the golden caption contains them. A real Noto subset retaining Arabic kaf but dropping Kurdish keheh passed the old guard and visibly split `کوردی` across fallback fonts. Both forms are now required and derived from `normalize_sorani` in the regression. The per-file check also had no product caller; `render_clip` now verifies the exact runtime `fonts_dir` and refuses empty/non-covering directories before publishing. |
| M3.2 | §4.3.6 golden-file render compared per build | DONE | `tests/golden/kurdish-caption.png` rendered on a verified libass build; compared on decoded pixels every gate run. `shaping=simple` must fail the same comparison — without that control the test measures nothing. D-021, `evidence/rtl-shaping.md`. **Adversarially audited 2026-08-08 (D-061):** the reference re-renders byte-identical here, `shaping=simple` differs by only 0.232% of pixels — small enough that any tolerance-based comparison would have to be tuned below it, which is why this one is exact — and CI has a dedicated step that fails if the test skips rather than runs. What the audit found missing was in invariant #4's cell, not this one: the golden line does not wrap, so it never evidenced the line-break claim. `evidence/rtl-shaping-wrapping.md`. |
| M3.3 | Stage 6 encode: crop/reframe + NVENC burn-in | PARTIAL | `src/hawedit/render.py` + `tests/test_render.py`. Cut, 9:16 crop, `shaping=complex` burn-in and encode all run and are verified on decoded pixels: a captioned render must differ from an uncaptioned one, and a `shaping=simple` render must differ from the shipped one. **NVENC now runs** — a real 1080×1920 clip encoded on hawapc01's 3090 Ti with captions verified in the NVENC pixels (`evidence/m3-3-nvenc.md`, `evidence/m3-3-nvenc-clip.mp4`). Getting there found a defect in the check itself: `encoder_available` probed at **64×64**, below NVENC's minimum frame, so the function written because a capability listing cannot be trusted called a working encoder unavailable — and `render_clip` refuses rather than substitutes, so NVENC would have raised on the one machine §6 puts it on. It probes at Stage 6's own output size now, and a GPU-free test pins that. D-045. **Shortfall (one, was two):** §3 Stage 6 reframes by tracking the active speaker from diarization plus face detection. **Corrected 2026-08-08 (D-069):** this cell said the crop "is static and says so by name, `Reframe.STATIC_CENTRE`, never `SPEAKER_TRACKED`" — a two-value description of a three-value enum, stale from the day `reframe.py` landed. `render.py` has carried `Reframe.FACE_TRACKED` since, and `render_clip` selects it whenever the tracker returns focus points. Face detection *does* run; only the diarization half is still missing (Community-1 measured **401** from here, `BLOCKED.md` #4), so the honest statement is face-aware-not-speaker-aware, carried with its own status in **M8.1** below. **Audited 2026-08-10 by adversarial pass #18 (D-133): 9/11, and this cell's own claim was one of the two that did not hold.** It said *full Kurdish coverage measured* for a set of **11** characters that did not include `ک` U+06A9 or `ی` U+06CC — and `normalize_sorani` converts Arabic `ك`/`ي` into exactly those, which §4.1 calls *the Farsi forms Kurdish uses*, so every normalized transcript is written in two letters no font was required to have. Both appear in this project's own `GOLDEN_CAPTION_TEXT`, and the comment above it claimed the line exercises *`ی` from §4.3.4's required set* while the set held no `ی`. Measured by subsetting the real shipped Noto to drop **only** U+06A9, keeping U+0643: `assert_font_covers_kurdish` **passed**, and the render changed by **15,999 subpixels** — libass fell back to another font for that character, so `کوردی` came apart into a detached, differently sized `ک` and `وردی`, a caption that looks entirely present while the viewer reads one word as two. Worse than §4.3.4's stated boxes, and the frame *gains* ink (8,367 → 9,267 lit subpixels). Set extended by two, derived from what the normalizer returns rather than from an alphabet — the same divergence from the frozen §4.3.4 list that D-013 took for `ھ`. **Second finding:** `assert_font_covers_kurdish` had **no caller in `src/`** — a build-time check its own docstring says must not be trusted, running at neither build time nor the burn, while `render_clip` burned whatever font sat in `fonts_dir` and `pipeline._runtime_fonts_dir()` resolves to an installed path no test has seen. `assert_fonts_dir_covers_kurdish` now runs in `render_clip` beside `assert_rtl_stack`. **Two more mutations survived and are now held:** the `--disable-libass`-beats-`--enable-` precedence, and `render_clip`'s own `assert_rtl_stack` call, which could be deleted unnoticed. **11/11** after. **Shaping and own-line-breaks survived the pass intact** (three mutations each, all caught). **Named shortfall:** the directory check does not verify that the font libass resolves *for the ASS's family name* is the covering one — family-name resolution is not reimplemented here rather than guessed. `evidence/adversarial-pass-18-2026-08-10.md`. **Corrected 2026-08-11 (D-167):** §4.3.5's *own line breaks* held for breaks this module inserts and not for one arriving inside a word. `Word` accepted any non-empty surface form, and an ASS `Dialogue:` event is **one line** — measured on real ffmpeg 8.1.1 and real libass, the frame burned from a word carrying a break is **byte-identical** to one rendered with the tail deleted (`b01fd8a7…` twice), while the intact render differs by 8,277 pixel bytes. The tail sits in `[Events]` as a line libass ignores and `parse_dialogue_times` returns *exactly* the intact file's times, so nothing downstream could see it. **Live, not latent:** `RawTranscript.from_json` accepted such a word, and `--transcript FILE` is a documented flag; canonical ASR cannot produce one because `_align` splits on whitespace. One guard at the chokepoint — `Word.__post_init__` requires `w.splitlines() == [w]`, not `len(...) == 1`, which would pass a trailing break. Rejected escaping it into a `\\N` (that invents a break the alignment never specified) and rejected refusing all whitespace (a space or tab loses no text). **3/3 lint-clean**, and the two threshold mutations discriminate: the length spelling reddens only the trailing-break case, the over-broad one only the four accepted cases. `evidence/a-word-that-was-not-one-line.md`. **Hardened 2026-08-12 (D-194):** a guard-revert sweep over every `raise` in this module — located by AST, deleted one at a time, whole suite each time — found **3 of 11 held by nothing**, and the worst was the last check between ffmpeg and a client: `if result.returncode != 0 or not output.exists()`. Deleting it left the suite green, and the next line probes the output for its duration, so a failed encode became whatever `probe_duration_ms` says about a file that may not be there. Reproduced with a **real** ffmpeg failure — the output path is a directory, exit **4294967283**, *“Error opening output …: Permission denied”* — and the test asserts ffmpeg's own words, not a generic message. Also held now: a source too small to crop (`1x1000`, `1000x1`, `2x2` all reduce to a zero dimension under `yuv420p` rounding) and a `0/0` frame rate (previously `ZeroDivisionError` from inside a rate probe). **No production code changed** — all three refusals were already correct. **3/3 mutations caught lint-clean**, file restored byte-identical. `evidence/three-refusals-in-the-renderer-that-nothing-would-miss.md`. |


## M5 — task ledger

| Task | Definition of Done | Status | Evidence |
|---|---|---|---|
| M5.1 | §3 Stage 2 visual index: scenes segmented to the reference settings, retrieval, and the top-50 → rerank → keep-5–10 contract | DONE | `src/hawedit/visual_index.py` + `tests/test_visual_index.py`. Runs in `pipeline.py` on real media: Stage 0's own cuts at 1400/2800 ms become three windows tiling 0→4162 ms, `evidence/m5-1-scene-windows.md`. The 64-frame ceiling and ~1 fps are enforced as one setting because either alone is satisfiable while the pair is broken — a 180 s scene at 0.35 fps is 63 frames, under the ceiling, and its embedding is indistinguishable from an honest one. A zero or NaN vector is refused rather than scored 0.0. The reranker may reorder and score; it may not invent a window, duplicate one, drop below the survivor count, or restate the retrieval score it was handed. D-037. **Amended 2026-08-09 (D-090), found by the third adversarial pass:** "may not drop below the survivor count" was checked against `len(index)` only, so the retrieval depth `k` walked past it — measured on a 60-window index with keep=5, `k=3` returned **3 survivors and no error**, `k=1` returned 1, `k=0` an empty tuple, and `k=-5` returned 5 after reranking **55** windows, because `retrieve` slices `scored[:k]` and a negative k drops the tail instead of keeping a head. `k < keep` is now refused as arithmetic rather than a threshold: retrieving fewer candidates than survivors cannot fill the slice, which is exactly what D-037 clause 4 forbids. Placed in `rerank_and_keep`, the only function holding both numbers — `retrieve`'s "top k" contract is honest at any k. 3/3 mutations, including the over-strict `k <= keep` boundary that only the control catches. **Scope:** `pipeline.py` never passes `retrieve_k`, so the shipped CLI always ran at §3's depth of 50 and could not reach this; the defect was in the public API and in clause 4's own wording. `evidence/survivor-floor-bypassed-by-k.md`. **The splitting path is exercised by tests only** — there is no long Kurdish episode here to run it against (`BLOCKED.md` #1). **Amended 2026-08-08 (D-052):** the plan takes one `fps` per media and nothing required the *index* to hold one — measured, the same footage at 1 vs 2 fps lands 0.117 apart in cosine distance against 0.186–0.224 between different scenes, so the rate was up to 63% of the signal. `VisualIndex.add` now refuses a second rate, with a positive control that a whole index above the reference rate is still legal (`evidence/m5-2-sampling-rate.md`). Which rate a real episode should use is open and needs `BLOCKED.md` #1. **Amended again 2026-08-08 (D-060):** that measurement's 4 fps arm was eight frames *read* of sixteen extracted, which is the missing explanation for the non-monotonicity it flagged and could not account for. The no-mixed-rates conclusion is unchanged and now overdetermined. **Amended 2026-08-08 (D-063, D-065):** the plan could emit windows `assert_frames_reached_model` refuses — at the 3 fps the CLI forced, a 30 s source planned two 45-frame windows and the model read **30** of each, and at 1 fps a 31 s media plans an odd count the processor pads by repeating the last frame. `SceneWindow` now bounds the rate above by the checkpoints' declared 2 fps as well as below by §3's reference, and `extract_window_frames` trims an odd emitted count to even, so the guard is unreachable by construction. Swept over 3 rates x 8 durations, every planned window is delivered whole; 4/4 mutations caught. Third re-measurement of the index: rank-1/rank-2 margin 0.0056 -> 0.0154 -> **0.0279**. `evidence/m5-1-declared-sampling-rate.md`. |
| M5.2 | Real `Qwen3-VL-Embedding-2B` / `-Reranker-2B` behind the interfaces above | DONE | `src/hawedit/qwen_visual.py` + `tests/test_qwen_visual.py` and `src/hawedit/video_input.py` + `tests/test_video_input.py`. Both models run on real media on hawapc01 `cuda:0`: an index over the fixture's three scenes, a Kurdish query retrieving against it, and the reranker **changing the order** (scene 0, third to second) while carrying every retrieval score through untouched — 8.17 GiB peak with both resident. The evidence index is at **3 fps**, not §3's ~1 fps reference — a deviation forced by D-049 (a 1400 ms scene is one frame at 1 fps) and recorded in D-052. **Corrected 2026-08-08 (D-060):** it was 4 fps until the processor was found to be re-sampling every window — the model received 4 of every 6 frames and nothing reported it, because the vector is unit-norm either way and the timestamps are computed from the rate we supplied. 3 fps is the only rate a 1400 ms scene is legal at. Every score moved (rank-1/rank-2 margin 0.005644 -> 0.015441) and the reranker now **reverses** retrieval rather than making one swap, so this row's central claim is better supported than before: `evidence/m5-2-frames-reaching-the-model.md`. `evidence/m5-2-embedder.md`, `evidence/m5-2-reranker.md`. Recipes are read from the checkpoints, never retyped: pooling, dimension, prompts, and the reranker's two score-token ids. Getting here found four silent defects — a 4162 ms window reaching the model stamped 0.1 s (D-049); a 1400 ms scene arriving as a single frame; a pooling recipe read and then ignored (D-050); and a logits shortcut whose bfloat16 error was **57% of the rank-1/rank-2 margin it was deciding** (D-051). **Three things this row does not claim.** The keep-5–10 survivor slice is unexercised on real media — the floor is five and the fixture has three scenes, so `rerank_and_keep` correctly refuses; it is covered synthetically. **Re-verified 2026-08-08 (D-066):** that refusal was reverted to `min(keep, len(reranked))` — returning however many windows existed — while this cell still certified it, and **I committed the reversal myself** in `3c270f7` by staging a whole shared file instead of HEAD-plus-my-edits. D-037 clause 4 had considered exactly that alternative and rejected it; no superseding entry was recorded. Restored, and moved before the reranker runs so a too-short media costs no GPU time. Whether either ordering is *good* is §8.2's question and needs `BLOCKED.md` #1. And D-050's cosine 0.955 against the loader the embedder's own checkpoint declares is still unexplained — it bears on retrieval quality, not on any number's shape. **Adversarial pass 2026-08-08 (D-053):** every claim in this row reproduced exactly, and **five of seven mutated guards passed the gate untouched** — including the whole D-049 timestamp fix and D-051's precision formula. The tests covered refusals reachable without weights and nothing covered the wiring. Four stub-level tests close it; the audit now reports 0 unprotected (`evidence/adversarial-m5-2.md`). **Re-verified 2026-08-08 (D-054):** the reranker's score reads `lm_head.weight` directly, so a randomly-initialised head would have made "the reranker reorders" a measurement of noise. Both checkpoints report `missing_keys: NONE` and tie `lm_head` to their embeddings — checked, not assumed. **Corrected 2026-08-09 (D-104), found by running the real 38-minute file:** the frame-count guard graded its own parity step's output. ffmpeg delivered 35 of a planned 36 frames — the one tail frame the guard's own message calls normal — the D-060 even-alignment step dropped a second, and the guard then compared 34 against 36−1 and raised "the window likely runs past the end of the media". The 35 files were on disk and the window sat early in a 2313.8 s file. Any window with an even plan and an odd delivery hit it — about half — and the visual path stopped after **three** windows; with the delivery judged before trimming it reached **164**. Rejected widening the tolerance to two, which a genuinely short window also satisfies. A guard I added alongside was **deleted** as unreachable, and the untested `> frame_count` ceiling now has a test. 4/4 mutations. `evidence/frame-count-guard-graded-its-own-output.md`. **Audited 2026-08-09 by adversarial pass #9 (D-117):** D-053's seven mutations were re-run on today's tree — the three call sites have since been consolidated into `window_batch` (D-060), so the mutations moved with them — and **7/7 CAUGHT**, including the five that were MISSED in August. The stub-level wiring tests hold. **One claim in this cell was stale:** it says the evidence index is *at 3 fps* and that *3 fps is the only rate a 1400 ms scene is legal at*. D-063/D-065 lowered the ceiling to `DECLARED_SAMPLING_FPS = 2.0` and re-measured at 2 fps (margin 0.015441 → **0.027870**); measured now, `SceneWindow` **refuses** 2.5, 3.0 and 4.0 fps. `evidence/m5-2-embedder.md` carries that supersession in a banner at the top of the file and this cell did not, so the published tally described a run at a rate the code raises on. The 3 fps figures above are the record of what was measured then, not what this code does now. `evidence/adversarial-pass-9-2026-08-09.md`. |
| M5.3 | §3 Stage 3 Path B: `VideoChat3-4B` over scenes, producing `Candidate`s the §3 union already accepts | DONE | `src/hawedit/path_b.py` + `tests/test_path_b.py`. Found a real defect in `clip.py`: `Sv6d` checked that a label *cites* a timestamp and could not check *which scene* — `speaker gestures at 9999s` (2.78 hours) constructed cleanly for a 12-second window (`evidence/m5-3-path-b.md`). `assert_sv6d_within_window` closes it, requiring *some* cited time inside the window so an honest duration alongside a moment still passes. **Amended 2026-08-09 (D-088), found by the third adversarial pass:** it closed the *bare* case only. Because a duration is a small number and any small number is inside a window starting near zero, pairing `9999s` with a matching duration rode the claim straight back in — measured, `'speaker gestures at 9999s, held over 1s'` was **ACCEPTED** on 0..1400 ms, and the same trick worked on 1400..2800 ("over 2s") and 2800..4162 ("over 3s"), i.e. on every window `plan_scene_windows` produces for this media. The cited tests used only 300000..312000, the one distance from zero where 1000 ms falls outside, so the rule bit exactly where it was tested and nowhere the pipeline runs — the same correct-but-blind fixture problem as D-086 one iteration earlier. Fixed without a threshold: a cited time outside the window is admissible only if it is **shorter than the window itself**, the longest duration anything inside it can have, derived from `in_ms`/`out_ms` rather than chosen. The docstring's legitimate label ("slow push-in over 3s, starting 5:04") still passes, and 3/3 mutations are caught including the over-strict direction, which only the control can see. Refusal tests are now parametrized over the three real windows. `evidence/sv6d-duration-smuggling.md`. §3's 256-frame budget is refused **before** the call, since past it the failure is an OOM kill rather than a wrong answer. **Corrected 2026-08-08 (D-069):** this cell said *"`run_pipeline(..., read_scenes=…)` makes the union two-sided over the windows Stage 2 planned on that video"*, and `pipeline.py:618` now **raises** on any non-`None` `read_scenes` — the seam was retired when `VisualComposer` took over the join, because an injected reader can promote every scene or hand back a score detached from its reranked hit. The correction had been recorded only in a prose block below this table, so the row itself still advertised a removed API. The two-sided union is real; it is composed through **M5.5**, not through this argument. D-039. **Amended 2026-08-08:** this row said the model was `BLOCKED.md` #2 and #6, both of which were resolved on 2026-08-08 and neither of which was ever the obstacle; M5.4 delivers the model and the prompt. **Corrected 2026-08-09 (D-118), found by running the real 38-minute file:** one survivor Path B could not read discarded **every other candidate**. `read_scenes` was `tuple(self.read_window(w) for w in windows)` with no per-window failure path, so a single `PathBError` aborted Stage 3 Path B — measured, 641 windows embedded, 50 retrieved, 7 survivors, and **0 candidates** because window 1 of 7 was a static logo card whose six well-formed dimensions cited a time **range** where `SV6D_PROMPT` asks for *the number alone*. The refusal is correct; the blast radius was not, and it is the shape D-103 fixed in Stage 1. A refused window is now an `UnreadableScene` carrying its bounds and reason, `discover_visual` returns `PathBDiscovery(candidates, unreadable)`, and the composer's report emits it with the empty case included. `discover_visual` still refuses when **nothing** was readable — the only bound needing no chosen threshold. The exactness guard was kept, not relaxed: *candidates ∪ unreadable == survivors*, so a model that silently omits a window is still refused. **The format is not the defect and that was measured:** 12 real windows read back through the real checkpoint gave **72/72 parseable lines, 12/12 point timestamps**, so widening `_LINE` to accept a range would be guessing whether the moment is its start, end or middle. 8/8 mutations — the first run was **7/8**, the defect itself surviving because every test used a fake reader and the real `read_scenes` was never driven (D-105/D-108/D-112 a fourth time). `evidence/one-window-discarded-every-candidate.md`. |
| M5.4 | Real `MCG-NJU/VideoChat3-4B` behind the Path B contract, and the SV6D prompt | PARTIAL | `src/hawedit/video_reader.py` + `tests/test_video_reader.py`. The model reads all three of the fixture's scenes through the real `discover_visual` — 0 on black, 1 on white, 2 on blue, six SV6D dimensions each, 49-60 s and 11.99 GiB peak, scored by Stage 2's reranker because §3 pairs the reader with *"embedding/rerank retrieval"* rather than asking a describer for a relevance number. `evidence/m5-4-path-b.md`. **The defect that would have shipped:** VideoChat3's own metadata type computes frame times as `video_start_time + index/fps` and then refuses `video_start_time >= duration`, so the offset a mid-media window needs is rejected by the checkpoint itself — every time it cites is window-relative while `assert_sv6d_within_window` reads media-absolute. Those agree for **exactly the windows starting at 0**, which is the one anyone checks; for the others an unshifted time is either loudly rejected or, when it happens to land in range, accepted and wrong by the window's start. The shift is now arithmetic in `build_sv6d` and the silent case is pinned as an explicit *accepted, and wrong* test (D-058). Getting here also found that VideoChat3 **refuses** Stage 2's `video_metadata` outright — `fps * duration must be equal to total_num_frames, but got 5.6 != 6` — fixed at the root after measuring the change byte-identical for Qwen3-VL (D-056), and that the timestamp guard's "half the window" bar encoded Qwen3-VL's frame pairing and **rejected all three windows for being correct**; it is derived from the frames now and still refuses the 24 fps failure 5.4–6× below (D-057). The first prompt wording returned a constant `0.0s:` on two windows and no timestamp at all on the third; the pipe-delimited form returned 18 of 18 lines parseable (D-058). Eight guards mutated one at a time, 8/8 caught — and the first run of that audit reported 8/8 against an already-red suite, recorded in the evidence because it measured nothing. **Shortfall (two).** The model cited `0.0` on every dimension of every window: correct for three static single shots, so the shift is demonstrated while temporal discrimination *within* a scene is unmeasured and needs footage where moments differ (`BLOCKED.md` #1). And `discover_visual`'s 256-frame budget sums the **whole set** before any call, which was right while Path B had no model and is now a ceiling on how many scenes a media may have — this reader calls once per window, so the VRAM figure the budget protects is per window. Left alone rather than loosened. **Re-run 2026-08-08 (D-060) at 3 fps** after the frame re-sampling was found: same three descriptions, same shift, and the 4 fps run that first produced this evidence is now refused by `assert_frames_reached_model`. **Measured 2026-08-09 (D-106), running the real 38-minute file:** the largest window this machine can read is **8 frames** at a 21.57 GiB peak — 90 % of a 23.99 GiB 3090 Ti, with 8.68 GiB of weights resident — and nine frames OOMs. §3 Stage 2 plans up to `MAX_FRAMES_PER_WINDOW = 64`, and the demand is **quadratic** in frames (48 → 196.44 GiB requested, 32 → 87.31, 24 → 49.11, 16 → 21.83, 12 → 12.28), so the gap is a factor of 64, not a margin. §3's 64 is **left alone** and now recorded canonically, so D-098's check refuses a future fix that quietly lowers it; truncating a planned window to 8 frames at read time is refused too, because D-104's guard exists for exactly that. The resolution — planning windows small enough for the reader — reshapes Stage 2's retrieval unit and is `BLOCKED.md` #17 with the full table. Stage 2's extraction, indexing, retrieval and reranking all ran: **164** windows reached the reader. `evidence/largest-window-a-3090ti-can-read.md`. |
| M5.5 | §3 Stage 2 → Stage 3 Path B composed under one owner: extract once, embed all, top-50, rerank all, keep 5–10, and show only survivors to VideoChat3 | PARTIAL | `src/hawedit/visual_pipeline.py` + `tests/test_visual_pipeline.py`. `VisualComposer.discover` owns the whole join and `pipeline.py` builds it behind `--visual`; the retired `read_scenes=` injection seam is refused at the door (see M5.3). A score on a Path B reading is accepted only when it is the exact reranker score for that same window, so a reading cannot arrive carrying a number nothing ranked. **This deliverable previously existed only as a prose amendment below this table with no status and no filename** — the composition the §9 M5 row calls "DONE in code" was not itself in the tally, which is what D-069 closes. **Shortfall (one, and it is the survivor floor).** Measured on the only media in this checkout: Stage 0's cuts at 1400/2800 ms plan **3** windows over 4162 ms, the real `QwenVisualEmbedder` embeds all three on `cuda:0`, and the composed path then **refuses** — `VisualPipelineError: visual retrieval refused this media: the index holds 3 windows and 7 survivors were asked for`. That is correct behaviour (§3 fixes the count at 5–10) and it means the composition's happy path is unexercised on real media here: VideoChat3 is never constructed, and peak VRAM is **4.08 GiB** — the embedder alone, confirming D-066's claim that the floor moved ahead of the reranker's weights rather than merely ahead of its call. Needs media with ≥5 scenes, `BLOCKED.md` #1 (`evidence/unlisted-modules.md`). **Corrected 2026-08-09 (D-105), found by running the real 38-minute file:** all three visual models took `--visual-device`, so §6's two-GPU video phase ran on one GPU — BLUEPRINT 346–347 puts Embedding/Reranker/TimeLens2 on GPU 1 and reserves GPU 0 for VideoChat3. Measured: GPU 0 held **18.30 GiB** with 3.59 free while GPU 1 held 1.3, and the run died on a 21.83 GiB allocation. Split per §6 (`--index-device`, default `cuda:1`), GPU 0 holds **10.44 GiB** with 12.18 free — 7.86 GiB freed. **It did not clear the OOM:** one 64-frame window (exactly `MAX_FRAMES_PER_WINDOW`) still wants 21.83 GiB beside 10.44 of weights on a 23.99 GiB card, so §3's ceiling and this checkpoint are in tension on a 3090 Ti. Lowering the cap would be a guessed threshold; the next step is to measure the largest window this GPU can read. The first version of the test asserted the parsed defaults and reverting either Qwen model left the suite green, so the composer construction was extracted and the wiring itself is now asserted. 5/5 mutations. `evidence/section-6-put-the-video-phase-on-one-gpu.md`. **Progressed 2026-08-09 (D-108):** the visual stage **ran on the real 38-minute file for the first time** — 641 windows indexed, 50 retrieved, 7 survivors, 7 candidates, both GPUs at 17,881 MiB, no OOM — once windows were planned to fit the reader. `plan_scene_windows` takes `max_frames` (exposed as `--visual-max-frames`), default §3's ceiling and only lowerable, with both bounds derived from recorded constants. The cost is measured, not hidden: 73 windows become **579** at a ceiling of 8, each seeing an eighth of the context, so §8.2's Recall@K is measured on a different retrieval unit than §3 describes — `BLOCKED.md` #17 keeps that open for Hawa. 5/5 mutations, after two survivors that were both *wiring* (D-105's lesson repeated). `evidence/planning-windows-the-reader-can-read.md`. **Corrected 2026-08-09 (D-117), found by adversarial pass #9:** the composed path's retrieval query was the **corpus**. With no `--visual-query` and no Path A candidate to anchor one, `pipeline.py` passed `normalized.text_ckb` — the whole normalized transcript — to `discover`. Measured by driving the real embedder on `cuda:1` (weights 3.96 GiB, card 23.99 GiB): 8,000 chars / 5,988 tokens is the largest query that fits, at a **9.86 GiB** peak; 16,000 OOMs; and this media's real **35,185 chars / 26,191 tokens** asks for **40.89 GiB** — the exact figure the composed 38-minute run died on. The demand is quadratic in tokens. It breaks both ways: where it does not fit the run dies in Stage 2 after Stage 0 has demuxed 38 minutes, and where it does fit — any short media, every fixture — every window is ranked against the whole episode and §8.2's Recall@K column gets a number that means less than it looks. Stage 2 now **refuses** with `blocked_by=("a retrieval query",)` before a frame is extracted, so a refusal costs no GPU time. Rejected truncating to a length (a guessed threshold that fixes only the crash) and a maximum length in `qwen_visual.py` (one machine's ceiling in a shared file — D-108's rule that no machine inherits another's limit). 5/5 mutations, with controls in both directions. `evidence/adversarial-pass-9-2026-08-09.md`. **Progressed 2026-08-09 (D-118):** with D-117's bounded query the composed path reached VideoChat3 on real media for the first time, and returned **0 candidates** — one of the seven survivors was unreadable and took the other six with it. `VisualDiscoveryResult.unreadable` now carries each refused survivor into the emitted report, and the composer's survivors-are-exactly-candidates guard became *candidates ∪ unreadable == survivors* rather than being relaxed. 8/8 mutations. `evidence/one-window-discarded-every-candidate.md`. **Progressed 2026-08-09 (D-119's run):** the composed path **finished on real media for the first time** — 641 windows indexed, 50 retrieved, **7 survivors**, **2 unreadable**, **5 candidates** with dense ranks and a real SV6D each, `discovery.by_path {"visual": 5}`. The rank-1 survivor (rerank **0.244822**, nearly 3× the next) was one of the two unreadable ones, so this run is also D-118's proof: before it, that one refusal discarded all seven. **The shortfall recorded above is closed in part and its citation was wrong:** the happy path needed *longer* media, not labelled media — `BLOCKED.md` #1 is the labelled corpus and governs whether these five are the *right* five, which is still unmeasured. `--auto-select` chose nothing, and that is not a defect: measured, the media's 185 complete sentences run 0.41–102.52 s with a median of **6.56 s**, the candidate windows are **3.38–3.88 s**, and **0** complete sentences lie wholly inside any of the five — 54 of 185 are short enough in principle, so it is circumstance rather than arithmetic. The retrieval unit being ~3.5 s instead of §3's ~32 s is `BLOCKED.md` #17. `evidence/the-report-shared-stdout-with-a-library.md`. **Audited 2026-08-10 (D-150), adversarial pass #23 — the row's own claims survived, Kurdish invariant #3 did not.** Both Stage 2 adapters §4.1-normalize the query before the model reads it, and removing either call, one at a time against a green baseline, left the **whole gate suite green**: `tests/test_qwen_visual.py` never mentioned normalization, `embed_text` was called by **no test at all**, and the two tests that call `score` pass `ڕۆژنامەوانی` — already normalized, so the call under test was a no-op where it did run. Measured on the codepoints, what that costs: `كوردي`→`کوردی` (`0x643`→`0x6a9`, `0x64a`→`0x6cc`), ZWNJ+`0x647`→`0x6d5`, `٢٠٢٦`→`2026` — none of which raises anywhere, so Stage 2 retrieves against a different alphabet from the one the corpus was indexed in, and the only symptom is a slightly wrong score. Now asserted on **what the model was asked to read**: `StubProcessor` records the conversation, one query carries four collisions at once, the assertions name the codepoints, and `_classes_taking_a_query()` reads the module for every class taking a `query` and compares it to the case list **both ways**, so a third adapter fails until someone drives it. The control is idempotence — an already-normalized query must arrive byte-identical, which catches an adapter that mangles or drops every query. Rejected asserting the returned vector (the difference is a small change in a unit-norm vector — exactly the symptom that hides — and the text test runs on CI without the 4 GB checkpoint) and normalizing at the caller (both docstrings already argue for inside, and the placement was never the defect). **8/8 mutations.** **Survived:** the three cited evidence files exist, and README's three claims for this module — pooling from the checkpoint, §7 role before the load, no silent CPU fallback — each still redden. Floor 1466 → 1471. `evidence/adversarial-pass-23-2026-08-10.md`. **Corrected 2026-08-12 (D-190):** this stage's `TEMPORAL_PATCH_FRAMES = 2` was justified by a comment stating as measured fact that *all four* §7 visual checkpoints ship `temporal_patch_size: 2`. Read off the four `video_preprocessor_config.json` files: `fps: 2` and `min_frames: 4` hold for all four, but **`MCG-NJU/VideoChat3-4B` ships `temporal_patch_size: 1`** and the other three ship 2. The constant stays 2 and nothing behaves differently — it is the **strictest** of the declared sizes, not a shared declaration, because `extract_window_frames` extracts once and D-140's cache hands the same files to the embedder and the reader, so the count must satisfy the coarser patch. Now pinned by **three tests that run on every machine**, asserting against a `DECLARED_VIDEO_PREPROCESSORS` table rather than reading the checkpoints — the first version read the files behind a `skipif` and **CI refused it**, because the floor compares *passed* and a weights-conditional test cannot count toward it (D-095). They assert `TEMPORAL_PATCH_FRAMES == max(recorded)` with a control that the sizes are not all equal, and that every §7 visual model appears in the table. **3/3 mutations**; before them all three constants could be set wrong with the suite green. `evidence/three-of-four-checkpoints-declare-what-the-comment-claimed-for-all-four.md`. **Corrected 2026-08-12 (D-186):** that last row is **not reachable** — `SceneWindow` enforces a 1.0 fps floor, so the real ceiling here is 8 frames @ 1.0 fps = **8.0 s**, which is still above the 6.72 s median sentence but currently crashes inside the reader (`BLOCKED.md` #22). No default changed: the sampling rate is a declared constant and picking a new one is a threshold decision. **5/5 mutations**, with one equivalent mutant reported rather than counted. `evidence/auto-select-chose-nothing-and-would-not-say-why.md`. **Adversarial pass 32, 2026-08-12 (D-193) — REFUTED.** `_EmbeddingCache` claimed reuse was verified “on everything that changes the vector” and keyed on three things: window, checkpoint, source digest. `extract_window_frames` trims the delivered frames to a multiple of `TEMPORAL_PATCH_FRAMES`, which D-190 defines as `max()` over the §7 checkpoints' declared `temporal_patch_size` — so it moves when the model set moves, with no edit to either file. Measured on `ZAR38MinTest.mp4`, window `zar38:s2:w0` (60,000–77,500 ms @ 2 fps): **34 frames** at a patch of 2, **32** at 4, different pixel digests, byte-identical key — and `discover` reads the cache *before* extracting, so nothing downstream could notice. The window's own `frame_count` does not cover it: that is the **planned 35**, which neither extraction produced. The trim is now in the record, read from the module that applies it; entries written before the key fail to match and re-embed once. The docstring now also names what stays outside the key — the ffmpeg invocation itself. **3/3 mutations caught**, file restored byte-identical; the first audit run reported 3/3 while every mutation was lint-dirty and `test_gate.py`'s real `verify.sh` was failing on ruff rather than on the guard. `evidence/adversarial-pass-32-the-reuse-key-did-not-cover-the-frames.md`. |

> **M5.5 amendment (D-168).** Stage 2's visual embeddings are now resumable per scene window.
> Each atomic record is keyed by the stable source SHA-256, the complete window identity, model
> id and pinned 40-hex checkpoint revision; malformed, linked, oversized, changed-source and
> changed-revision records re-embed rather than rank stale vectors. The emitted run records cache
> hits and misses. On the real 38-minute source, the exact hash-locked Windows GPU profile and
> pinned Qwen embedding checkpoint computed five windows once (36.176 s), then served 5/5 hits
> with zero new frame-embedding calls (8.266 s, including verified model reload/query); all five
> cache records were byte- and mtime-identical after the second pass.
> `evidence/stage-2-embedding-resume.md`.
>
> **M0.1 amendment (D-169).** All nine `[project.scripts]` entry points now derive argparse's
> program name from the actual invocation. Installed launchers (including Windows `.exe`) print
> the command a user ran; `python -m` prints the pasteable module form. The tests derive the
> module/script mapping from `pyproject.toml` and exercise both launcher suffixes plus module mode
> for every declaration. `evidence/help-names-the-invoked-command.md`.
>
> **M0.1 amendment (D-170).** README now states the live strict required-`gate` protection and
> its `cli.py` module-map row names every exported shared entry-point rule. Tests bind the former
> symmetrically to `BLOCKED.md` #7 and the latter to `hawedit.cli.__all__`.
> `evidence/readme-quality-bar.md`.
>
> **M0.1 amendment (D-171/D-173/D-175).** The readiness branch now contains protected-main
> history. All 27 upstream-only commits were semantically classified first; missing behavior
> landed on the stronger tree, and merges `89a1641`, `8128707`, then `ccb11a3` joined the
> corresponding main parents with trees exactly equal to their readiness first parents.
> `evidence/main-semantic-merge-2026-08-10.md`.
>
> **M0.1 amendment (D-172).** `BLOCKED.md` resolution parsing now recognizes its complete,
> declared heading vocabulary: `RESOLVED` and `ANSWERED`. The suite refuses an undeclared heading
> marker, a declared word unused by the ledger, and the concrete regression where answered #10
> reads as live. `evidence/a-blocker-could-resolve-invisibly.md`.
>
> **M5.4 amendment (D-059).** The whole-episode 256-frame shortfall named in that row is
> closed: windows are now packed into deterministic ≤256-frame calls. The unmeasured temporal
> discrimination on real dynamic Sorani footage remains a shortfall.

## M6 — task ledger

| Task | Definition of Done | Status | Evidence |
|---|---|---|---|
| M6.1 | §3 Stage 5 TimeLens2 contract: intervals as evidence, never as cuts, and only where they are about the clip | PARTIAL | `src/hawedit/timelens.py` + `tests/test_timelens.py`. **Demoted from DONE 2026-08-09 (D-085), found by the second adversarial pass — the row's two halves have different verdicts.** *Evidence, never cuts* was **true but untested**: §3's formula names `timelens_interval_end` in `final_out` and nowhere in `final_in`, and adding the interval's start to the in-point candidates left the whole suite green. Pinned now, with a control that the out point still moves. *Only where they are about the clip* is **false**: `interval_for_fusion` accepts any overlap at all, so **1 ms** qualifies — anchor 10000..14000 with evidence 13999..305000 fuses a **295.0 s** clip from a 4.0 s sentence, and through the real `run_pipeline` a 1.60 s anchored sentence shipped as **4.10 s** (2.56×), attributed to `timelens_interval_end`. The runner's uncaptioned-speech guard catches this only when unselected *words* sit in the swallowed span — applause, music and untranscribed tails have none, which is exactly the case. **Shortfall (one, named):** the bound is unset and §3 gives none (it bounds the shot cut at 400 ms and says nothing here); three candidate rules with opposite failure modes are recorded in `BLOCKED.md` #15, and choosing by taste is what the never-guess-a-threshold rule forbids. `test_one_millisecond_of_overlap_currently_qualifies_as_relevant` pins the measurement until it is answered. `evidence/timelens-relevance-unbounded.md`. Found a real defect in `boundary.py`: `timelens_interval_end_ms` arrived as a bare integer, so §3's "latest of { …, timelens_interval_end }" read as `max()` over the episode — measured, a 4.0 s anchored sentence became a **295 s clip with Kurdish invariant #2 still passing**, because the invariant constrains direction and nothing constrained relevance (`evidence/m6-1-timelens-relevance.md`). Eligibility is now overlap with the anchored sentence, checked in the selector **and** at the fusion site, because `BoundaryInputs` accepting a naked end is what let it happen. D-038. |
| M6.2 | Sentence-hard fusion (the HARD/SOFT rule itself) | DONE | Delivered in M2.2 — `boundary.py`, invariant #2 swept over 78,125 combinations covering every sign of all seven optional `BoundaryInputs` fields (D-078; it was 3,125 over five of seven, omitting `natural_silence_ms` and `media_duration_ms`). **Audited 2026-08-10 (D-155), adversarial pass #24 — 7/10, and all three survivors were the same shape: an invariant cannot see a *choice*.** This row's evidence is the 78,125-combination sweep of Kurdish invariant #2, which proves `final_in <= anchor_in <= anchor_out <= final_out` for every combination of signs. §3 Stage 5's SOFT rule is also a **selection** — *earliest of* / *latest of* — and every wrong selection still moves the edge outward, so the sweep cannot fail on one. Measured: every existing shot-cut test supplies **exactly one cut**, the input where `min` and `max` agree. With three cuts inside the 400 ms window, taking the nearest preceding cut instead of the earliest starts the clip **300 ms late, mid-shot** (9650 → 9950), and taking the nearest following cut leaves the 200 ms tail as winner and ends it **150 ms early** (14350 → 14200) — both with the invariant intact and 1,501 tests green, and both against the frozen blueprint's own words. Pinned, each with a control that discriminates. The third survivor is a **bad mutation of mine**, recorded rather than pinned: `cut <=` → `cut <` is unobservable because `min` returns the first minimum and the anchor is appended first (measured — a cut on the anchor gives `in_extended_by=None`), so a test would pin a tie-break §3 does not specify. Rejected extending the sweep to cover selection: a sweep is for invariants, a choice wants a case. **Survived:** every constant §3 names (120/200/400 ms), dropping the tail, dropping either optional candidate, and the clamp at 0. **9/9 after**; no production code changed. `evidence/adversarial-pass-24-2026-08-10.md`. |
| M6.3 | Real `MCG-NJU/TimeLens2-4B` behind the contract above | PARTIAL | `src/hawedit/video_grounding.py` + `tests/test_video_grounding.py` and `VisualEvidenceInterval.from_window` + `tests/test_timelens.py`. The model grounds real scenes: asked for `a red number 2 on a blue background` it declines scenes 0 and 1 and localises scene 2 to 2800..3600 ms — 3 windows in 18.8 s, 8.53 GiB peak, `missing_keys: NONE`, and it needs none of VideoChat3's three workarounds. `evidence/m6-3-grounding.md`. **The defect that would have shipped:** its answers are on the *window's* clock while `boundary.py` fuses media-absolute ms, so the real answer `[[0.0, 0.8]]` about scene 2 reads unshifted as 0..800 ms — inside scene 0. Through the real selector and real fusion at an anchor of 0..400 ms that makes the clip 200 ms longer **and records `timelens_interval_end` as the reason** for footage 2.8 s away, with invariant #2 satisfied because it constrains direction not relevance — M6.1's own defect through the offset instead of through `max()`. At 0..600 the number coincides and only the attribution is wrong, which §8.2 reads provenance off. The shift is in `from_window`, beside the type, so a second producer cannot omit it (D-062). Also found: TimeLens2 is the only §7 visual checkpoint with **`do_resize: false`**, so frames must be 32-px aligned — `align_to_patch_grid` uses the checkpoint's own `smart_resize` (640x360 -> 640x352) and `load_window_images` now takes the processor everywhere; and the first `parse_spans` refused `[]`, which is what the model returns for a scene the query is not in, so the commonest correct reply would have crashed on the first real episode. **Shortfall (three).** Not wired into `run_pipeline`: §3 Stage 5 must choose what to ground against — the anchored sentence, a Path A candidate, the verdict — and `ground` refuses an empty query rather than inventing a default. Whole-media grounding was **wrong** on this fixture (asked about the blue 2 over all 4162 ms at 1 fps it answered `[[1.0, 2.0]]`, the white 1) — four frames is close to no temporal structure, so this fixture cannot say whether the model is accurate, only that the adapter is honest; §8.2, `BLOCKED.md` #1. And the gate: a concurrent session held this checkout red all iteration, so the change set was proved in a worktree at HEAD plus these files (ruff/format/mypy clean, suite exit 0 excluding `test_gate.py`, whose 9 failures reproduce identically at plain HEAD there). |

> **M6.3 composition amendment.** The runner now grounds only scene windows overlapping the
> selected candidate, using the selected canonical transcript slice as the non-empty query. It
> fuses only intervals relevant to the sentence anchors. The remaining shortfall is real-footage
> accuracy, not pipeline wiring (`BLOCKED.md` #1).
>
> **Corrected 2026-08-10 (D-165).** The earlier “Not wired into `run_pipeline`” sentence in the
> historical row above is superseded by this amendment and by the current runner: it calls
> `ground_all` only for overlapping windows, fuses the returned media-time intervals, and closes
> the grounder after the phase. The row remains PARTIAL solely because accuracy lacks the labelled
> real-footage acceptance set. The same audit found that a directly constructed `SceneWindow`
> accepted `../../outside`; Qwen, the composed visual pipeline and TimeLens each translated only
> the ID's colons before using it as an extraction directory. `SceneWindow` now enforces the shared
> portable media-ID contract at construction, so every consumer receives a path-safe logical ID.
> `evidence/adversarial-pass-20-2026-08-10.md`.

> **Adversarial production-hardening amendment (D-105–D-110).** Human review can no longer be
> replaced by an automatic QC flag; hosted-model numeric fields are exact-type and finite; every
> frame extraction owns a private namespace; render duration has a one-frame upper as well as
> lower bound; canonical transcript publication is serialized for readers and competing writers;
> and known visual component refusals reach the runner's `StageSkipped` report. Reproductions and
> executable controls: `evidence/adversarial-production-hardening.md`.

| M3.4 | §8.3's third render-regression bullet: the boundary invariant asserted on every **shipped** clip | DONE | `render.py` (`assert_encoded_span`, `frame_duration_ms`) + `boundary.py` (`media_duration_ms`) + `tests/test_render.py`, `tests/test_boundary.py`. The invariant had been asserted on the `Clip` object; `RenderResult.duration_ms` was the request echoed back and the file was never opened. Measured: requesting 8000 ms of a 4162 ms source makes ffmpeg exit 0 and write 4180 ms. The new check immediately caught the runner's own end-to-end fixture — §3 Stage 5's 200 ms tail pushed `final_out` 138 ms past the end of the file, so **every `run_pipeline` render had been silently truncated** while the suite stayed green (`evidence/m3-4-shipped-clip-invariant.md`). D-040. **Audited 2026-08-09 by adversarial pass #8 (D-147):** four of six mechanisms held, including the original defect — echoing the request back is caught, and an independent ffprobe of a real render agreed with the repo's own at 4162 ms. Two did not. `assert_encoded_span(duration_ms, duration_ms, …)` — the guard comparing the request against itself — left the suite green, because the function is unit-tested but only reachable through `render_clip`, and truncation by a short source is prevented upstream, so nothing drove it. And `frame_duration_ms` returning a constant **40** survived against its own docstring, because every fixture is 25 fps where 40 is correct. Both now tested — the wiring by driving `render_clip` into a short measurement with a control for the exact case, the rate by generating a 30 fps source (**33 ms** against 40). 6/6 after the fix. `evidence/adversarial-pass-8-2026-08-09.md`. |

| M3.5 | Captions timed to the clip, and a burn that refuses an ASS with nothing to draw | DONE | `captions.py` (`clip_in_ms`, `parse_dialogue_times`, `assert_captions_within_clip`) + `tests/test_caption_timing.py`. **The most serious defect found so far.** `build_ass` wrote source-absolute timestamps and `render_clip` burns into a stream already cut at `clip.in_ms`. Measured on a 1.6 s clip taken from source 2000 ms: **0 bytes** differ between a captioned and an uncaptioned render — libass drew nothing, ffmpeg exited 0, and the output is a valid, playable, caption-free MP4. Kurdish invariant #4 was absent from every clip not starting near zero. The existing pixel test is the right test; its fixture cuts at 300 ms with words at 0–1600, the one input where the bug is invisible. `evidence/m3-5-caption-timeline.md`, D-041. **Audited 2026-08-09 by adversarial pass #12 (D-125):** all eight mechanisms held — the shift itself, both out-of-window refusals in `build_ass`, the empty-Dialogue and all-outside refusals in `assert_captions_within_clip`, partial overlap being enough, the guard running at the burn on whatever file arrives, and `WrapStyle: 2`. **The gap was where they are proved.** Measured: with the `- clip_in_ms` shift removed, `tests/test_render.py` and `tests/test_pipeline.py` report **0 failures** — every catcher is in `tests/test_caption_timing.py`, whose pixel proof builds the ffmpeg command by hand, so the composition never ran through `render_clip`. Not a fault in those tests: `_write_ass` hands the renderer `build_ass((_sentence(),))` and `_sentence()` is 0..1600 **in clip time**, which is what a renderer should be given; and `run_pipeline`'s clip starts at ~100 ms, where an unshifted caption still overlaps and draws. This row's own words describe the trap one level down — *the one input where the bug is invisible*. A test now drives §4.2 source-time sentences through `build_ass(clip_in_ms=clip.in_ms)` and the real `render_clip` at **2000 ms** — derived, because `_sentence()` is 1600 ms long so an unshifted caption falls entirely outside the window and can be refused, where at 500 ms it still draws — asserted on decoded pixels, with a control that the unshifted file is **refused at the burn** rather than encoded bare. 8/8 before and after; nothing was broken, and the proof now runs through the function the product calls. `evidence/adversarial-pass-12-2026-08-09.md`. |

| M3.6 | §2's delivery set complete: SRT and EDL alongside the MP4, ASS and §5 JSON | DONE | `src/hawedit/delivery.py`, `src/hawedit/artifact_bundle.py`, `src/hawedit/atomic_fs.py` and real-media pipeline tests. SRT uses the clip timeline; EDL source fields use the source timeline while record fields start at zero. NTSC 30000/1001 and 60000/1001 write SMPTE drop-frame labels (two and four skipped counts per non-tenth minute respectively), tenth-minute/hour boundaries, `FCM: DROP FRAME`, and equal quantized source/record durations; other unsupported fractional rates refuse. **Amended 2026-08-09 (D-083):** the five files are staged privately, flushed, validated as one exact non-empty set, and published by a single directory rename. The public render result is withheld until that succeeds. Write failures leave no public partial set; a two-worker race yields one complete unmixed winner. **Hardened 2026-08-10 (adversarial pass 23):** root, staging and artifact identities are lstat/descriptor-bound; hardlinks and reparse roots are refused; and delivery/checkpoint publication share a native atomic no-replace primitive, so a racing empty destination cannot be replaced. `evidence/adversarial-pass-23-2026-08-10.md`. `evidence/m3-6-delivery-set.md`, `evidence/m3-6-drop-frame-edl.md`, `evidence/m3-6-high-frame-rate-drop-frame.md`, `evidence/atomic-delivery-bundle.md`, D-042/D-082/D-083. |
| M3.7 | Reproducible, provenance-bound wheel publication | PARTIAL | `src/hawedit/release.py` + `tests/test_release.py`. `hawedit-release` requires clean Git `HEAD`, derives `SOURCE_DATE_EPOCH` from the commit, builds twice, refuses unequal wheel bytes, validates runtime data, and atomically publishes a write-once release directory. **Amended 2026-08-09 (D-084):** that directory includes deterministic SPDX 2.3 JSON covering the exact wheel checksum, bundled Noto font checksum/OFL relationship, and every declared base/optional dependency from wheel METADATA. It does not pretend unbundled requirements are resolved. `SHA256SUMS` covers the wheel, SBOM and provenance; the independent `spdx-tools 0.8.5` parser/validator accepts the result. **Amended 2026-08-09 (D-090):** the two builds no longer inherit an ambient backend. The command creates a private builder from exact, official-wheel-hashed Pip 26.2.1 and Setuptools 84.0.0 requirements, refuses drift against `[build-system]`, and records the measured Python/frontend/backend plus lock digest in provenance. Measured on one clean revision, two formerly allowed Setuptools versions emitted different wheel hashes and 68.2.2 could not build at all (`evidence/release-builder-lock.md`). Remote CI actions are full-commit pinned (`evidence/ci-actions.md`, `evidence/release-sbom.md`). **Amended 2026-08-15 (D-239):** strict `vMAJOR.MINOR.PATCH`, exact accepted-SHA pre-tagging, draft byte verification, forward-only rollback, and a fresh no-checkout immutable GitHub Release job are contract-tested; repository release immutability is live (`evidence/versioned-immutable-release.md`). **Accepted 2026-08-15:** exact-main release run `31875765513` built, smoke-tested and OIDC-attested all four payloads for `ef40ff7c`; independent Windows CPython 3.11/3.12 clean installs also passed. **Shortfall:** no production tag or durable public release exists until the human-approved exact `v0.1.0` tag is created and the guarded workflow is rerun. CPU base/gate/model-fetch/GPU transitives and WSL distribution identities are hash-locked; native WSL build outputs are not bit-reproducible. Deterministic source-to-wheel output, component disclosure, production-gate binding, project-managed checkpoint bytes and package-managed OmniASR bytes are closed—not the entire external supply chain. `evidence/current-main-acceptance-2026-08-15.md`. |

> **M3.7 dependency amendment (D-094).** Every direct GPU/cloud dependency is exact and the
> vulnerable Pytest 8.3.4 development pin is now 9.1.1. One clean host-extras
> (`dev,media,cloud,gpu`) Python 3.12
> environment passes 1,237/1,237 with zero skips. This closes direct-version drift, **not**
> transitive hash locking; that remains part of M3.7's supply-chain shortfall.

> **M3.7 checkpoint amendment (D-096).** The five accessible project-managed Hugging Face snapshots
> now have an exact packaged byte manifest enforced before load. This reduces that part of the
> external-model supply chain; it does not close gated pyannote identities, release signing,
> or transitive environment locking. Package-managed OmniASR identities are closed separately in
> D-101 below.

> **M3.7 promotion amendment (D-100).** Publication now requires an explicit successful official
> `main` push run of `.github/workflows/gate.yml` for the exact clean release SHA. The API, exported
> Python function and CLI all fail closed before creating a builder or output directory when the
> run, repository, workflow, branch, event, SHA, attempt, job or any mandatory step is wrong. The
> exact run/job is recorded in schema-5 provenance. Redirects are refused before authorization can
> cross hosts. Both wheel builds consume separate pristine `git --no-replace-objects archive`
> exports of the verified SHA, so live-worktree races and build-1 residue cannot enter or stabilize
> the artifact. `evidence/release-exact-gate.md`.

> **M3.7 OmniASR amendment (D-101).** The canonical 43.5 GB LLM-7B/CTC-3B/tokenizer set now has
> exact packaged URL/cache-key/size/SHA-256 identities enforced during atomic setup and before every
> model load. Runtime also pins and hashes the official card document, disables system/user card
> sources, validates the effective bare tokenizer reference and refuses extra cache members or a
> tampered ready worker snapshot. Verified descriptors remain open and are the paths consumed by
> both real fairseq2 model loaders; stale readiness is invalidated before setup mutation. Unlimited-3B
> remains absent rather than being claimed from a card name alone.
> `evidence/omniasr-asset-integrity.md`.

> **M3.7 authenticity amendment (D-113).** A successful official `gate` push on `main` now feeds a
> permission-separated release workflow. Repository code builds with a read-only token; fresh
> no-checkout 3.11/3.12 jobs install and execute the exact wheel; a final fresh job independently
> validates the exact four-file transport, binds schema-5 provenance to the event, and alone
> receives GitHub OIDC/attestation authority. Attestation and final upload receive the same
> explicit allowlist. The workflow and actions are locally linted and contract-tested, but the
> live hosted acceptance gate remains open until this workflow is on default `main` and all four
> downloaded files pass `gh attestation verify`. `evidence/release-attestation.md`.

> **M3.7 Python/install amendment (D-116).** The supported host range is honestly capped at Python
> 3.11–3.12 because the pinned base graph and official ASR stack do not resolve on 3.13. The
> required `gate` cannot pass until a full 3.12 zero-skip prerequisite passes, and release
> attestation cannot start until fresh no-checkout 3.11/3.12 venvs install the exact wheel, pass
> `pip check`, resolve package data and start all nine CLIs. Local current-wheel smokes pass; hosted
> execution awaits default `main`. `evidence/python-support.md`.

> **M3.7 host-lock amendment (D-122).** Twelve code-digest-bound locks cover base, canonical
> gate and isolated model-fetch environments on Linux/Windows CPython 3.11/3.12. Setup, both CI
> Python versions and release smoke install only exact wheel hashes and audit the full inventory;
> wheel data is located and authenticated through raw `RECORD`, including real `pip --target`
> relocation. Windows 3.11/3.12 and source/wheel/model-fetch paths are measured; Linux is
> resolver-verified and exercised by CI. CUDA remains a separate open lock.
> `evidence/host-dependency-locks.md`.

> **M3.7 artifact-identity amendment (D-157).** Reproducible bytes are no longer sufficient if the
> wheel identifies a different project or version. Release now requires the archived
> `pyproject.toml`, the single wheel METADATA record and the PEP 427 filename to agree on one
> normalized distribution name and exact version before publication. Schema-5 provenance records
> both fields. The fresh privileged workflow job independently opens the wheel without a checkout,
> requires distribution `hawedit`, rechecks filename/METADATA equality and binds those measured
> fields to provenance before OIDC attestation. This closes artifact-identity substitution.
> D-239 subsequently defined the version/tag and immutable-publication policy; its live hosted
> release acceptance remains open. `evidence/release-identity-binding.md` and
> `evidence/versioned-immutable-release.md`.

> **M3.7 archive-byte amendment (D-185).** Windows `git archive` no longer rewrites packaged text
> data: every tracked `.txt` is pinned to LF, and a forced-autocrlf regression compares all 15
> archive members to their committed blobs.  The exact `9322f28` wheel built twice identically and
> passed fresh CPython 3.11/3.12 hash-only installs, `pip check`, exact environment audit, seven
> installed data probes and all nine CLI helps.  Hosted attestation still depends on protected
> main. `evidence/release-text-byte-integrity-2026-08-10.md`.

| M1.8 | Windows private checkpoint staging and hard-crash resume | DONE | `src/hawedit/windows_security.py` + `src/hawedit/model_fetch.py` + `tests/test_model_fetch.py`. Fresh directories receive protected native DACLs at creation; root/member owner and ACEs are revalidated, `Everyone:F` is refused, and one revision-specific private tree survives hard process death. `evidence/checkpoint-provisioning.md`, D-124. |
| M3.10 | Code-bound host dependency locks | DONE | `src/hawedit/host_lock_hashes.py` + `src/hawedit/environment.py` + twelve `requirements/host-*.txt` locks + `tests/test_host_dependencies.py`. CPU base, gate and model-fetch environments are exact wheel-hash graphs for Linux/Windows Python 3.11/3.12. `evidence/host-dependency-locks.md`, D-122. |
| M3.8 | Package-managed OmniASR model/tokenizer byte and card-policy integrity | DONE | `src/hawedit/omni_assets.py` + `tests/test_omni_assets.py`, integrated by `asr.py` and `wsl_setup.py`. Exact identities, atomic verified first download, no-follow full rehash, descriptor-bound real model loading, private empty card sources, effective metadata equality and atomic ready-worker identity are enforced. `evidence/omniasr-asset-integrity.md`, D-101. |

| M3.9 | WSL-ASR dependency vulnerability disposition gate | DONE | `src/hawedit/vex.py`, `src/hawedit/wsl_audit_locks.py`, `src/hawedit/wsl_vex_gate.py`, `security/wsl-asr-vex.json` and their tests. The protected hardware path installs an exact 29-wheel scanner in an isolated WSL environment, requires pip-audit 2.10's exact object schema, revalidates the canonical receipt/assets, and evaluates captured policy bytes bound to the complete 140-distribution identity, lock digests, assets and HawEdit source digest. On 2026-08-09 the live two-GPU Ubuntu runtime accepted all 12 audited findings against 12 explicit dispositions; this is affected/mitigated evidence, not a vulnerability-free claim, and the policy expires 2026-09-08. `evidence/wsl-asr-live-acceptance-2026-08-09.md`, `evidence/wsl-asr-vex.md`, D-123/D-142. |

> **M1.4/M3.8 runtime-receipt amendment (D-111/D-112).** WSL readiness is now a schema-2
> receipt over one exact worker snapshot and one versioned/revalidated venv generation, not a
> plain flag over a shared mutable environment. Setup is serialized across processes; launch
> re-hashes source and live-probes the recorded interpreter, packages, assets and CUDA route;
> `ModelStore` no longer equates importability with readiness. Predictable transcript/setup/model
> locks refuse link/reparse/replacement attacks and use explicit long Windows retry.
> `evidence/wsl-runtime-receipt.md`.
 **Corrected 2026-08-09 (D-114):** §4.3.5 — *insert line breaks yourself from the word alignment, because automatic wrapping on RTL text produces bad break points regardless* — reached the ASS and not the SRT. `build_ass` wraps through `wrap_caption_lines`; `build_srt` wrote `sentence.text`, which is `" ".join(words)`, so the sidecar shipped one line per sentence and the break points belonged to whatever wraps the cue at playback. Measured on the real 38-minute Sorani run through real OmniASR: **149 of 182** sentences short enough for a clip (81.9%) were wider than the 32-char caption line, median **104 chars = 4 lines**, widest 973 = 33 lines — every one of them shipped as a single SRT line. Both formats now share one wrapper at one width, pinned to each other by a test; a wider SRT line was rejected as a guessed threshold and a two-line cap as unspecified overflow. The ffmpeg round-trip added as an independent reader turned out **not** to enforce the blank-line rule — that mutation round-trips byte-identical — so it is documented for what it proves and the hazard is pinned elsewhere. 6/6 mutations. `evidence/the-srt-let-the-player-choose-the-break-points.md`. **Hardened 2026-08-10 (D-146):** the set was atomic against a failing *write* and not against a **kill**. D-072's `except (DeliveryError, RenderError, OSError)` never sees a `KeyboardInterrupt`, and a `SIGKILL` runs no clause — so its recorded claim to *close* D-071's delivery-atomicity shortfall held only for the case it tested. Measured on a real run interrupted at the second of the three sidecar writes: `.ass`, `.mp4` and §2's `.json` manifest on disk with **no SRT and no EDL**, no report at all because the process died, and then the retry into the same directory raising `FileExistsError` — D-071's guard refused when *any* of the five paths existed, so the files the interrupted run stranded were what made the retry impossible. One Ctrl-C ended that work directory. Now: every sidecar and the ASS are written through `_write_atomic` (staged `.<name>.tmp`, one rename), and `{clip_id}.delivery.provenance.json` is written **last** with each artifact's byte length — the guard refuses only a set whose record exists *and* matches, and a missing record, unreadable record, missing file or wrong length all route to redoing the work (D-132's shape). `PipelineRun.resumed_over` names what was overwritten, in the emitted JSON, `[]` on a clean run. Rejected digests (a length catches truncation and absence for free; a digest would re-hash the MP4 on every guard call and only adds tamper detection, which a killed process does not produce), mtime staleness (a guessed threshold) and deleting the leftovers (every writer already overwrites). **Accepted narrowing, named:** two *simultaneous* runs of the same media id and selection into one work directory are no longer caught at the pre-write guard — a best-effort defence that already failed if the second run got there first, traded for the certainty that an interrupted run is recoverable. **10/10 mutations** after 9/10; the survivor let a missing artifact fall through the record check. Five existing tests changed and all five got stronger — `_existing_artifact` now plants a *finished* delivery via the production writer instead of one file. Floor 1427 → 1434. `evidence/delivery-set-not-atomic-against-a-kill.md`. **Corrected 2026-08-10 (D-165):** the sidecar shipped cues no player can read in order. `build_srt` refused an incomplete sentence, one starting before the clip and one ending after it — every check about a sentence *on its own*, none about the sequence. Measured by reading the written file back: cues out of order shipped, overlapping cues shipped, and **`00:00:00,900 --> 00:00:00,400`** shipped — a cue whose end precedes its start, built entirely from words that are individually valid, since `Word` refuses `end_ms <= start_ms` but `Sentence` derives its bounds from `words[0]` and `words[-1]` with nothing requiring the tuple sorted. Never computed rather than computed and discarded. `assert_deliverable_order` now lives in `sentences.py` and **both** writers call it — `pipeline.py` hands the same sequence to `build_srt` and `build_ass`, so a guard on one would burn the overlap into the video while the sidecar refused it. Touching exactly (`later.start_ms == earlier.end_ms`) stays allowed: that is consecutive speech, and it is what two of the new tests control for. Rejected sorting the input instead of refusing it (a silent correction the next reader cannot detect) and rejected checking it in `Sentence.__post_init__` (an unsorted sentence is a legitimate intermediate; the property belongs at delivery). **6/6 mutations**, each half of the check and each call site mutated separately; two were re-run after the first pass orphaned an import and partly measured ruff. `evidence/the-sidecar-shipped-cues-a-player-reads-in-order.md`. **Corrected 2026-08-10 (D-166):** D-165's own exception could not be caught by the block that calls its raiser. `build_srt` raises `UndeliverableOrder`; the delivery block catches `(DeliveryError, RenderError, OSError)`, and those two are **siblings** under `ValueError`, not parent and child. Demonstrated against the tuple copied verbatim from `pipeline.py`: **ESCAPES**, skipping the cleanup that keeps the set all-or-none (D-072) and the `StageSkipped` that names the gap. **Latent, not live:** `build_ass` runs first on the same sentences and its handler catches `ValueError`, so nothing wrong shipped — but that is an ordering guarantee, not an exception contract, and this block was reordered once already. The handler now names the type, and `test_the_delivery_handler_catches_everything_its_builders_refuse_with` reads the `except` clause **by AST out of `pipeline.py`** so the requirement is derived from the tuple that actually protects the writes. Rejected making `UndeliverableOrder` a `DeliveryError` subclass (circular import: `delivery` imports `sentences`) and rejected widening to bare `Exception` (it would swallow what this stage should not). **1/1 on the defect**, lint-clean; two further mutations were lint-dirty and are recorded as contaminated rather than counted, and the surviving sibling assertion is documentation rather than a control — the state it would catch cannot be built. Also probed and sound: `build_edl`, whose source-versus-record duration property was already pinned. `evidence/an-exception-the-delivery-handler-could-not-catch.md`. **Swept 2026-08-12 (D-194):** every `raise` in `delivery.py` deleted one at a time against a green baseline, whole suite each time — **12/12 held**, each by a test named for the property (negative timestamps, unreadable cue timings, an empty SRT, an incomplete sentence, a sentence outside the clip, non-integer and non-positive frame rates, a negative in-point, a zero-length clip, a clip shorter than one frame). A null result, recorded because the sweep's value is as much in the modules it clears as in the ones it does not. `evidence/three-refusals-in-the-renderer-that-nothing-would-miss.md`.

## M7 — task ledger

| Task | Definition of Done | Status | Evidence |
|---|---|---|---|
| M7.1 | §8.2 metrics: per-path Recall@20, temporal IoU, sentence-completeness, misleading-edit rate, pairwise preference, cost/wall-clock per source hour | DONE | `src/hawedit/repurposing.py` + `tests/test_repurposing.py`. `path_unique_wins` answers §8.2's collapse question directly. Definitions: D-020. **Amended 2026-08-09 (D-077), found by the adversarial pass:** for a gold set with no winners `recall_at_k` returned `None` and `recall_at_k_by_path` returned `{}` — both meaning unmeasured — while `path_unique_wins` returned `{verbal: 0, visual: 0, both: 0}`, which §8.2's collapse test reads as licence to delete a path. Unmeasured now returns `{}`, matching the sibling; a *measured* zero is still reported for every path, because that zero is the collapse finding, and the control pins it. **Gap opened by D-077 and closed by D-080:** every recall surface and the Stage 3 merge now refuse thresholds outside `(0, 1]`, including zero, NaN, infinities, booleans and strings, before any empty-set return; Recall@K also refuses non-positive, boolean and fractional K. Zero is excluded because disjoint spans have IoU zero and would otherwise match. This validates configuration without claiming the threshold is tuned—M7.3 still depends on the labelled set. `evidence/metric-parameter-validation.md`, `evidence/iou-threshold-validation.md`. |
| M7.2 | 200–500 human-reviewed candidates labelled per §8.2 | BLOCKED | Needs human annotators and real footage — same dependency as `BLOCKED.md` #1. **The harness for the set exists and the set does not** (named here as of D-069): `src/hawedit/editorial_bench.py` + `tests/test_editorial_bench.py` load a labelled set, evaluate it and emit a promotion report, exposed as `hawedit-editorial-bench`. Measured: **0 labelled sets on disk**, and `assert_real` refuses an empty one (`editorial set has 0 items; the promotion floor is 20`) and refuses an interim one outright (`an interim editorial set cannot be reported as production evidence`), so the harness cannot be made to print a number it did not measure. Note the two floors are different questions: `MIN_REGRESSION_ITEMS = 20` is §3's bar for *promoting a judge model*, while this row's 200–500 is §8.2's bar for *tuning thresholds* — clearing the first would not close this row. `evidence/unlisted-modules.md`. |
| M7.3 | Threshold tuning against the labelled set | TODO | Depends on M7.2 — an unstarted task, not an external blocker. Every threshold in `DECISIONS.md` marked "awaiting real data" is tuned here. |

## M8 — task ledger

§9's M8 is *"Auto-reframe (SAM 3 / Molmo2)"*, and both models remain `BLOCKED.md` #9. §3 Stage 6
says to add SAM 3 **only if** face-centred cropping proves insufficient on real footage, so the
face-centred attempt is M8's own prerequisite rather than a substitute for it, and it gets a row.

| Task | Deliverable | Status | Evidence |
|---|---|---|---|
| M8.1 | Face-centred vertical tracking — §3 Stage 6's prerequisite for judging whether SAM 3 is needed | PARTIAL | `src/hawedit/reframe.py` + `tests/test_reframe.py`. `OpenCvFaceTracker` samples at 2 fps, picks a continuous dominant face through `choose_face`, smooths the path over 5 samples and drives a time-varying crop expression; `render_clip` labels the result `Reframe.FACE_TRACKED` when points come back and `Reframe.STATIC_CENTRE` when they do not, so the artifact never claims tracking it did not do. Wired into `pipeline.py` behind `--face-reframe`. **This row previously existed only as a prose amendment under M3.3 with no status and no filename** — invisible to the tally and to any test, which is what D-069 closes. **Shortfall (two).** Measured on the only footage in this checkout — `tests/fixtures/kurdish-speech-3cuts.mp4` — the tracker returns **0 focus points**, correctly, because the fixture is coloured digits and contains no face. So the fallback is exercised on real pixels and the tracked path is not; whether the smoothing holds on a real speaker is unmeasured and needs `BLOCKED.md` #1 (`evidence/unlisted-modules.md`). And `choose_face` associates nothing to *speech*: with two faces it follows the larger/more continuous one, which §3 Stage 6 explicitly does not sanction — active-speaker association needs Community-1 (`BLOCKED.md` #4). Not a SAM 3 substitute; §9's M8 stays blocked. |

**M3.3 / M8.1 amendment — 2026-08-15 (D-241):** the code-solvable association seam now
exists. `SpeakerSubjectTracker` receives only exclusive turns overlapping the fused final clip;
every returned point must match the speaker active at that media-clock instant. Valid evidence is
recorded as `crop_target=speaker_face` and `Reframe.SPEAKER_TRACKED`. An empty result is explicit
ambiguity and may fall back to face-only/static; invalid output, runtime failure, missing
diarization, or no overlapping turn refuses visibly. The renderer independently rejects a mode
whose points contradict it. The canonical Windows/Python 3.12 gate passed **2,478/2,478, zero
skipped** twice, including all six named integration/render cases. Status remains **PARTIAL**:
there is no production associator or CLI path, the Community-1 checkpoint is still gated, and no
labelled multi-speaker footage has measured association or crop accuracy.
`evidence/speaker-face-association-seam.md`.

## Production-hardening amendments - 2026-08-09

- **M0.1 / D-117:** the earlier import-token repair was insufficient: a purpose-built executable
  could forge it. The gate now accepts only this checkout's path-identical canonical `.venv`, then
  verifies editable-root/distribution identity, supported Python/project versions and exact active
  requirements. A clean Python 3.12.13 environment passes; the shared stale venv is correctly
  refused. `evidence/environment-identity.md`.
- **M1.4 / M3.8 / D-119:** the WSL source receipt now includes the exact trusted checkpoint
  manifests used by Qwen-ASR. Prior valid receipts remain usable during/after failed reprovision;
  result publication is random, no-follow, single-link and descriptor-bound.
  `evidence/wsl-runtime-receipt.md`.
- **M1.6 / D-120/D-124:** resumed checkpoint trees are recursively validated before any third-party
  downloader writes through them. Fresh roots are unpredictable, then atomically become a
  revision-specific 0700/protected-DACL resume tree which survives hard process death. Planted
  hardlinks and real Windows `Everyone:F` access are refused before download.
  `evidence/checkpoint-provisioning.md`.
- **M1.6 / D-121:** `hawedit-fetch-models` is now shipped in the wheel. The checkout shell file is
  a thin wrapper around the same transaction; neither path installs or upgrades its own download
  client. Installed-package metadata remains the trust root while the model directory contains
  only mutable weights. `evidence/checkpoint-provisioning.md`.
- **M1.6 / M3.7 / D-150:** `hawedit-ffmpeg-setup` and the authenticated pinned provisioner are now
  shipped in the wheel. Linux can provision into a receipted per-user generation; Windows/macOS
  receive bounded package-manager remediation, and `--check` never downloads. A fresh no-checkout
  Windows wheel install passed its packaged hash lock, `pip check`, RECORD-authenticated script
  lookup, and real RTL capability probe. No external FFmpeg binary is redistributed.
  `evidence/installed-ffmpeg-provisioning.md`.
- **M2.7 / M2.8 / M5.5 / M6.3 / D-118:** expected ASR, Path A, Path B, keyframe, judge, TimeLens,
  render/delivery and tracker failures stop at their named stage. Empty auto-selection performs no
  Stage 4 work; candidate traversal is refused before filesystem use; lazy credential/model
  failures make no transport/billed call. Every exception-derived JSON reason is printable and
  bounded, while assertions/schema errors remain visible. `evidence/pipeline-failures.md`.
- **M2.7 / D-155:** Stage 0 now obeys the same structured-failure contract. Expected ingest and OS
  failures produce one bounded ingest refusal plus eight explicit downstream skips rooted at
  `Stage 0 ingest`; `--json` remains parseable and exits 1. Static/schema/programmer errors still
  take the exit-2 exception path. `evidence/stage-0-failure-reporting.md`.

These amendments close code-solvable failure/reporting gaps. They do **not** close CUDA/GPU
dependency locking, WSL native-build provenance/live VEX enforcement, real Sorani/editorial
measurements, confidential Vertex acceptance, the real 5-or-more-scene visual happy path,
pyannote access, or real-speaker reframing.

## Upstream adversarial-pass amendments - 2026-08-09

- **M0.8 / D-161:** §8.1 alignment accuracy now reaches the published benchmark report instead of
  ending on each `ItemScore`. The aggregate is weighted by matched words, carries coverage and its
  exact tolerance, emits `None` when unmeasured, and refuses mixed thresholds. Six timed items and
  an out-of-threshold control exercise the real report boundary.
  `evidence/section-8-1s-last-metric-never-reached-the-report.md`.
- **M0.7 / D-160:** published benchmark aggregates now have exact absence controls: no scored items
  emits `None`, never zero, for mean/worst RTF; no VRAM probe emits `None`, never zero, for peak
  memory. Opposite controls preserve measured RTF and 17 GiB values, and the unprobed case is driven
  through `run_benchmark` rather than a fabricated report. `evidence/adversarial-pass-17-2026-08-09.md`.
- **M2.7 / D-159:** the suite now contains a real `run_pipeline` result for which every completeness
  requirement is satisfied, then proves that a named skip, no visual windows, or no candidates each
  makes it incomplete. Stage 5's `BoundaryInputs` are also bound to the cuts Stage 0 measured from
  the same source. The stale “four blocked stages” claim is retired; a bare run currently names
  eight. `evidence/adversarial-pass-16-2026-08-09.md`.
- **M2.6 / D-158:** Stage 4's managed-migration and billing boundaries are now pinned at the exact
  edges: a 20-item tie cannot promote, an 11-to-10 win can, 200,000 tokens is refused while 199,999
  passes, and 21 keyframes are refused while exactly 20 in-span frames pass. The production contract
  was already correct; the previous tie test was answered by the sample floor and matched `ties 0`.
  `evidence/adversarial-pass-15-2026-08-09.md`.
- **M0.1 / D-125/D-127:** canonical environment identity is now paired with gate-tool origin
  validation, and the passed-test floor is idempotent when legitimate skips exist. A forged
  `pytest` can neither mint JUnit evidence nor poison the ratchet.
- **M0.4 / D-139:** every claimed write-once transcript enforcement route is reached by its own
  mutation-sensitive regression; the ledger no longer counts prose as an enforcement layer.
- **M0.7/M0.11 / D-135:** one failed ASR region becomes `UnalignedSpeech` instead of deleting a
  completed episode. Successful regions still pass through confidence/disagreement routing and
  preserve validator provenance. Real Sorani accuracy remains blocked on the labelled corpus.
- **M0.9 / D-126/D-130:** serialized benchmark reports must carry the exact per-dialect values that
  justify their aggregate, and decision thresholds are pinned to their executing constants.
- **M1.4 / D-131/D-132:** checkpoint readiness requires both exact verified bytes and a loadable
  architecture runtime; the human report is derived from structured status, including zero sizes.
- **M2/M3 / D-133:** `editing.json` carries the actual verbal/visual/union discovery path rather
  than inferring one from the presence of transcript text.
- **M1.4 / D-134:** every WSL invocation uses the shared `--exec` prefix, including setup, receipt
  probes, Stage 1 worker launch and the live vulnerability gate.
- **M5.4 / D-136/D-137:** raw ffmpeg delivery is checked before parity trimming inside its private
  attempt directory, and the visual phase now assigns VideoChat3 to GPU 0 while Stage 2 and
  TimeLens use GPU 1.
- **M5.3 / M5.5 / D-156:** one schema-invalid VideoChat3 survivor is now a named
  `UnreadableScene`, not a failure that erases every readable survivor. Candidate IDs union
  unreadable IDs must still exactly equal the reranked survivor set, and an all-unreadable call
  still refuses. `evidence/one-window-discarded-every-candidate.md`.
- **M5.4 / D-138 remains PARTIAL on acceptance, not composition:** the production 3090 Ti reads at
  most eight VideoChat3 frames (eight succeeded at 21.57 GiB; nine OOMed). Composed Path B now
  plans gap-free scene windows to that measured capacity while preserving the blueprint's general
  64-frame ceiling; reader-side truncation remains prohibited. The remaining proof is a real
  full-episode rerun measuring retrieval quality, cost and peak memory with the increased window
  count.
- **Ledger / D-128/D-129:** unenforced per-file test counts were removed as claims, and promotion
  evidence distinguishes real reviewed checkpoints from test stubs.

## M2.7 query-capable auto-selection amendment (2026-08-10, D-177)

Protected main measured a real 38-minute `--visual --auto-select` run spending about 170 seconds
before reporting that Path B had no retrieval query. Readiness's stricter earlier CLI contract
already refused that exact invocation before Stage 0; the auto-selection predicate and structured
skip now encode the same capability directly. Path A can produce alone, while Path B requires
`--visual` plus a normalized nonempty `--visual-query`. Seven behavioral tests cover both positive
routes and every refusal.

The concurrent interrupted-delivery delta is semantically superseded by the hidden, exact-set,
one-rename `ArtifactBundle`, whose crash-retry and concurrent-publisher tests remain green.
`evidence/auto-select-query-preflight-2026-08-10.md`.

## M2.8 confidential-ZDR class-set amendment (2026-08-10, D-179)

Protected main proved that disabling Vertex's confidential ZDR gate reddened nothing and, under
that mutation, recorded both billed endpoints receiving the client's transcript and real source
pixels. Production was already correct. The coverage now constructs every concrete judge under
all forbidden confidential states, asserts zero transport, separates ZDR configuration from
attribution, and holds `count_parts` plus `generate_json` independently. Positive controls keep
the matrix from passing by refusing everything. Thirteen new cases pass in the 78-test Gemini
suite. `evidence/confidential-zdr-class-set-2026-08-10.md`.

## M2.7 CLI-preflight amendment (2026-08-10, D-181)

Protected main proved that most prerequisite tests could pass after deleting their target guard
because they asserted only the shared exit code 2. Readiness now has 21 real-CLI cases covering all
15 reachable preflight guards, including both sides of compound rules. Every case asserts its own
diagnostic and that Stage 0 created no work directory; a bidirectional AST contract catches future
uncovered guards and stale cases, while a legal-argv control proves the block does not refuse
everything. Two dominated branches with no CLI-reachable state were removed rather than certified
through internal-only fixtures. The full pipeline suite passes 143/143.
`evidence/cli-preflight-boundaries-2026-08-10.md`.

## M5.2 Stage 2 query-normalization amendment (2026-08-10, D-183)

Protected main proved that removing §4.1 normalization from either the Qwen embedder or reranker
left the full suite green while silently comparing different alphabets. The test processor now
records the actual conversation for both adapters: raw Arabic kaf/yeh, ZWNJ and Arabic-Indic digits
must be absent; normalized Sorani and ASCII digits must be present. An idempotence control and a
bidirectional query-reader class inventory prevent uniform mangling and future untested adapters.
The focused visual slice passes 95/95. `evidence/stage2-query-normalization-2026-08-10.md`.

## M0.4 invariant #1 digest-evidence amendment (2026-08-10, D-186)

The missing/unreadable sidecar refusal is now held separately from digest mismatch.  Five ways the
evidence can disappear or become invalid are derived into tests through direct integrity checking
and normalized-artifact publication, then repeated after raw-byte tampering; an intact control
must still verify and publish/read.  Production was already correct.  This is
deletion/corruption detection, not a signature against an actor who can rewrite both files.
`evidence/invariant-1-digest-evidence-2026-08-10.md`.

## M2.6 billed-smoke preflight amendment (2026-08-10, D-188)

The live Gemini smoke now refuses missing/nonexistent video before confirmation and before either
billed Path A call. README documents the required flag and the absent matching 13-second recording
as `BLOCKED.md` #19. `evidence/smoke-video-preflight-2026-08-10.md`.

## M2.9 keyframe-timestamp amendment (2026-08-10, D-189)

Partial ffmpeg output is stamped from the requested cadence rather than stretched across the full
span. A real 13-second request over 4.162-second media returns genuine partial JPEGs with no
timestamp beyond source end. `evidence/keyframe-timestamp-cadence-2026-08-10.md`.

## Delivery-audit claim binding (2026-08-10, D-190)

Protected main corrected a stale statement about its older flat-file recovery path. Readiness
uses a newer publisher: incomplete work stays in a unique hidden directory and the exact five-file
set becomes visible through one no-replace directory rename. Importing main's wording would have
made this audit false in the opposite direction. The adapted regression executes both current
guard outcomes and binds the live audit text to them. A second derived check requires every
`D-NNN` citation in each root document to resolve in `DECISIONS.md`.
`evidence/main-delivery-audit-adaptation-2026-08-10.md`.

The protected-main history is now joined by merge `5a9099a` (D-191). Its first-parent tree and
merge tree are both `ef2a73250462f8835b5d2a65f617753bb73ebd7c`; the final post-join gate is
still required because identity of Git trees does not execute the program.
`evidence/main-delivery-audit-merge-2026-08-10.md`.

## Deferred with reason

| Item | Deferred to | Reason |
|---|---|---|
| ~~§5 clip contract~~ | ~~M1–M2~~ | **Delivered in M2.2** — boundary fusion gave it a producer. Kurdish invariant #2 is enforced at fusion and again at the render gate. |
| Conjunctive `و` separation (§4.1) | M1 | Not implemented by KLPT `normalize` — measured, see `DECISIONS.md` D-003. |
